# V&VN Data Services — Protocol

## Geldende norm

De geldende normatieve baseline is Protocol v2.23.0 plus Protocol v2.22.0 plus Protocol v2.21.0 plus Protocol v2.20.0 plus Protocol v2.19.0 plus Protocol v2.18.0 plus Protocol v2.17.0 plus Protocol v2.16.0 plus Protocol v2.15.0 plus Protocol v2.13.0 plus Protocol v2.12.0 plus Protocol v2.11.0 en bestaat uit:

1. [Protocol v2.2.0](docs/PROTOCOL_V2_2.md) — lifecycle, provenance en acceptance;
2. [Protocol v2.3.0](docs/PROTOCOL_V2_3_TECHNICAL_DELTA.md) — setup, stack en kostentransparantie;
3. [Protocol v2.4.0](docs/PROTOCOL_V2_4_PRODUCT_DISTRIBUTION_DELTA.md) — product, distributie en extern gebruik;
4. [Protocol v2.5.0](docs/PROTOCOL_V2_5_MVP_PUBLIC_REMOTE_DELTA.md) — MVP-uitzondering voor een publieke gezaghebbende remote;
5. [Protocol v2.6.0](docs/PROTOCOL_V2_6_INTERNAL_OPERATIONS_CONSOLE_DELTA.md) — interne operations console als menselijk oppervlak over de knowledge kernel;
6. [Protocol v2.7.0](docs/PROTOCOL_V2_7_SOURCE_API_DISTRIBUTION_DELTA.md) — first-wave bron, object-level retrieve-and-abstain API en distributie;
7. [Protocol v2.8.0](docs/PROTOCOL_V2_8_USERS_HIERARCHY_CONSOLE_ORDER_DELTA.md) — primaire gebruikers, bronhiërarchie (klasse × familie) en console-bouwvolgorde;
8. [Protocol v2.9.0](docs/PROTOCOL_V2_9_CONSOLE_UX_BRAND_DELTA.md) — taakgerichte onderzoeker-UX en V&VN digitale stylesheet voor de console;
9. [Protocol v2.10.0](docs/PROTOCOL_V2_10_CONSOLE_NAV_ACCOUNTS_DELTA.md) — Documentenhierarchie, wachttaak-badges en Accounts-kamer (gesloten rollenset);
10. [Protocol v2.11.0](docs/PROTOCOL_V2_11_HTML_FREEZE_LOCATOR_DELTA.md) — geüploade HTML-freeze, weigering van live URL-HTML, verplichte source locators en fail-closed Product API zonder locator;
11. [Protocol v2.12.0](docs/PROTOCOL_V2_12_OBJECT_TYPE_REVIEW_PROJECTION_DELTA.md) — objecttype (unclassified-default), reviewbinding en atomaire gepubliceerde projectie;
12. [Protocol v2.13.0](docs/PROTOCOL_V2_13_ATOMIC_OBJECT_SEMANTICS_DELTA.md) — atomaire objecten, per-type classificatieregels, gesloten relaties en high-risk four-eyes;
13. [Protocol v2.15.0](docs/PROTOCOL_V2_15_INGEST_DATE_VERSION_REVIEW_LANES_DELTA.md) — ingest-brondatum/versie (kalender + ISO, dotted-integer), heading-voorstel als reviewbaan-voorwaarde, reviewlijst-snippet en type-gebaseerde reviewbanen;
14. [Protocol v2.16.0](docs/PROTOCOL_V2_16_REVIEW_PAGE_RESEARCHER_BAR_DELTA.md) — reviewpagina-onderzoekerbar: één deur Beoordeel, twee stacks Koppen/Inhoud met counts, compacte brontekst-rijen, DOEN/OVERWEEG/NIET DOEN als stempels, geen tiny objects, nieuwe extract van unpublished Continentie;
15. [Protocol v2.17.0](docs/PROTOCOL_V2_17_REVIEW_PAGE_RESEARCHER_SURFACE_DELTA.md) — reviewpagina-onderzoekersoppervlak: geen slogans / geen EPD MAG (gehele zin «Dit wordt wat een EPD MAG zeggen.» MAG weg), geen via-negativa-help op onderzoekerskamers, leeg Onderwerp op verse ingest, bronpassage als leesbaar proza op ieder object / de hele freeze, geen kennisplatform-chrome (ook geen één-woord Tools) als Koppen/objecten, stempel-UI alleen op recommendation, compacte relatiecheckboxes, re-extract unpublished Continentie.
16. [Protocol v2.18.0](docs/PROTOCOL_V2_18_REVIEW_CARD_EXTRACT_DEDUP_DELTA.md) — reviewkaart toont de freeze-zin één keer (MUST NOT als h3/titel én body); extract MUST NOT een grammaticale voortzetting / naloopzin tot een nieuw object splitsen; extract MUST NOT identieke clean_text-passages als extra objecten emitteren omdat de freeze-HTML ze herhaalt; re-extract unpublished Continentie;
17. [Protocol v2.19.0](docs/PROTOCOL_V2_19_REVIEW_DUTY_QUEUE_DELTA.md) — onderzoeker-reviewplicht en wachtrijpresentatie: onderzoekers MUST NOT 2008 Inhoud-kaarten één voor één openen; Koppen blijven batch-bevestigbaar als structuur; trage plicht is voorgestelde recommendation plus condition/exception/high-risk; resterende unclassified MUST NOT als gelijke duizenden-kaarten-plicht worden gepresenteerd; 2008 Inhoud blijft een fail van dit reviewoppervlak;
18. [Protocol v2.20.0](docs/PROTOCOL_V2_20_UNPUBLISHED_DOCUMENT_DELETE_DELTA.md) — unpublished-documentverwijdering: PROTOCOL.md is de wet voor iedere richtlijn, niet Continentie-only; Continentie in v2.16–v2.19 blijft live bewijs van fails, niet de productidentiteit; unpublished captured snapshots MAGEN van de operations console worden verwijderd door een geautoriseerde console-operator; MUST een echte verwijdercontrole op de documentkaart / Review-chooser voor unpublished snapshots alleen; MUST bevestigen vóór uitvoering; na delete MUST de snapshot weg zijn van Inleveren/Review/Documentenhierarchie; objecten+envelope van die snapshot_id zijn weg; MUST audit-ledgerrij; MUST NOT gepubliceerde projectie verwijderen; MUST NOT fragmenten verbergen in een freeze die in Review blijft; MUST NOT SSH/wipe van /home/data als productpad.
19. [Protocol v2.21.0](docs/PROTOCOL_V2_21_CONTROLLED_USE_WAVES_DELTA.md) — controlled-use golven A→B→C→D: eigenaar pauzeerde Cloud Shell; prioriteit bronintegriteit, heldere kennisobjecten, veilige omgevingsscheiding, herstelbaarheid; Cloud Shell / production ZIP blijven uit tot golf A op een gecontroleerde SHA staat; golf A (volgende implementatie only) context-bewuste splitter + toetsbare reject-functie; inhoudelijk kennisobject MUST één complete, zelfstandig leesbare betekeniseenheid zijn met bronpassage+locator; MUST NOT alleen nummer/label/kopwoord/nav/stempel/zinsfragment; MUST NOT identieke clean_text uit dezelfde freeze; Inleiding is geen chrome; Home/Tools/Richtlijnen/Meedenken zijn chrome; Continentie-regressiefixtures; geen infra in golf A; golf B G2-statusbewijs (MUST NOT stale static JSON; publicatiegate MUST NOT openen omdat een app-setting aanwezig is; claimt geen G2 PASS); golf C PR #82 afmaken, niet activeren tot test-App Service bestaat; golf D backup/herstelbaarheid; MAPT bestaande wet, herschrijft die niet; v2.20 unpublished-delete is GEEN vijfde golf. Golfdefinities blijven wet, behalve de in v2.22 begrensde volgende-implementatievolgorde A-B-C-D.
20. [Protocol v2.22.0](docs/PROTOCOL_V2_22_WAVE_ORDER_C_D_BEFORE_B_DELTA.md) — begrensde supersessie van Protocol v2.21 §3-volgorde A daarna B daarna C daarna D, alleen voor volgende-implementatievolgorde; golven zelf ongewijzigd; historische v2.21-golfdefinities blijven wet; golf A staat al in code op `main` `512ffa5026d06ff804434ddf4d07a08a36c02305`; volgende implementatie MUST golf C daarna golf D; MUST NOT deploy-test/deploy-production activeren tot Azure test-App Service `vvn-metis-console-test` bestaat; merge naar `main` MUST NOT automatisch deployen naar een ontbrekende test-app; ZIP MUST volledig deploybaar zijn inclusief dependencies; git-archive-only is niet genoeg; `--clean true` wist wwwroot en MUST NOT runtime-data verwijderen; DAARNA één Cloud Shell / production ZIP van die gecontroleerde SHA (A+C+D); geen live-URL-ingest; ingest van een nieuwe freeze MUST NA die ZIP, niet ervoor; live draait nog pre-golf-A extract tot ZIP; golf B (G2-bewijs/smoke) NA die ZIP; G2 blijft BLOCKED; `publish()` blijft G2-BLOCKED; ZIP opent publicatie niet; claimt geen G2 PASS; MUST NOT SSH-wipe van `/home/data`; MUST NOT C/D in deze PR implementeren; v2.20 unpublished-delete blijft op main, GEEN vijfde golf; golf C (PR #82 afmaken, niet activeren) ÉN golf D, daarna stoppen voor William Cloud Shell ZIP van die SHA, daarna ingest; geen G2 PASS, geen Blob-grant.
21. [Protocol v2.23.0](docs/PROTOCOL_V2_23_CODE_SURFACE_SIMPLIFICATION_DELTA.md) — code-oppervlakte-vereenvoudiging: eigenaar vroeg Auditor om een code-oppervlaktereview op `main` HEAD `a566af56c8c88e76cb4de7fa51642b408705da02` en het resultaat aan Metis (PROTOCOL/ROADMAP-eigenaar) te overhandigen; Auditor-oordeel REQUEST SIMPLIFICATION; geen GD-03, geen publicatie; G2 blijft BLOCKED; deze delta MUST NOT het live-pad wijzigen; volgende live-stap blijft William Cloud Shell ZIP van A+C+D SHA `a566af56`, daarna ingest van een freeze, daarna golf B (G2-bewijs); vereenvoudiging is NA die ZIP, niet in plaats daarvan; PR #82 blijft gesloten/ongemerged; golven A, C en D staan al in code op die SHA; eerste DELETE-snede, zero-caller src/-modules only: extract_pdf.py, semantic_transform.py, validation_workflow.py, build_second_review_queue.py, pre_step5_gate.py, import_expert_validation.py, reconcile_legacy_review.py, evaluate_safe_retrieval.py, build_retrieval_document.py; MUST NOT `src/semantic_transform_v2.py` verwijderen; MUST NOT `src/prepublication_gate_v2.py` verwijderen; MUST NOT `src/validation_workflow_v2.py` verwijderen; MUST NOT `src/apply_second_review.py` verwijderen (subprocess-lock `tests/test_protocol_v2.py` tot die v2.0-lock een expliciete follow-up is); MUST NOT `src/canonical_store.py` verwijderen; service_app.py EN product_api_v1.py blijven (inspectie versus Product API; twee producten, geen leftovers); live ingest gebruikt extract_pdf_v2, extract_html_v1, semantic_transform_generic_v1, prepublication_gate_v3; operations_console_v1.py versus operations_console_app.py split blijft; geen template-engine; golf A splitter + reject_candidate blijven; test_protocol_v2_* en test_v2* blijven beide; nieuwe eligibility-regels gaan alleen in eligibility_policy; MUST NOT high-CC fail-closed functies splitsen; scripts/run_integrity_sprint.sh MAG in dezelfde of de volgende wijziging naar committed fixtures wijzen i.p.v. `python -m src.semantic_transform_v21`; MUST NOT v2/v21/generic mergen in die PR; CLI review-queue versus console review_stacks / slow_review_duty is dual live path (plan, MUST NOT stilzwijgend verwijderen); Console is de onderzoeker-plichtwachtrij; volgende implementatie NA die ZIP is één deletion-PR, bestaande pytest only, MUST NOT splitter of console in die PR raken; Implementation MUST opnieuw bewijzen dat er geen live callers zijn vóór iedere delete; HANDOFF.md MUST NOT opnieuw worden aangemaakt; MUST NOT deletion-PR in deze PR implementeren; claimt geen G2 PASS.

- Status: goedgekeurd voor projectgebruik
- Goedgekeurd: 2026-09-03
- Eigenaar: projecteigenaar V&VN Data Services
- Regel bij conflict: de strengste fail-closed eis geldt. Protocol v2.5 is een begrensde supersessie van v2.2 §4.1 (private remote MUST) uitsluitend voor de gedeclareerde MVP-periode op `WilliamGomes41/VENVN-DS`. Protocol v2.6 is een begrensde supersessie van de v2.4-lezing «geen productfrontend in deze repository»: een interne operations console MAG in deze repository; De console is goedgekeurde scope, niet bestaande code; een zorgapp-frontend, chatbot, EPD/ECD-UI en publieke website blijven verboden. Protocol v2.7 is een begrensde supersessie van v2.4 §10 (geen modeltraining in het MVP): training MAG alleen als tweede licentie, en alleen als de client DS op vraagmoment nog aanroept om de gepubliceerde status te controleren. Protocol v2.8 is een begrensde supersessie van v2.6 §7 en v2.7 §2 uitsluitend voor de bouwvolgorde console-versus-Fase-2 en de eerstvolgende concrete taak: de volgende implementatie is een echte console-MVP op de bestaande kernel; duurzame immutable opslag wordt niet overgeslagen. Protocol v2.9 is een begrensde supersessie van v2.8 §6 uitsluitend voor de eerstvolgende implementatie nadat de console-MVP bestaat: die volgende implementatie is een console-UX-rewrite op de bestaande kernel (taakgericht onderzoekersoppervlak + V&VN digitale stylesheet); duurzame immutable opslag wordt niet overgeslagen; de G2-locator blijft de publicatieblocker. Protocol v2.10 is een begrensde supersessie van v2.9 §10 uitsluitend voor de eerstvolgende implementatie nadat die UX-rewrite in gang is: die volgende implementatie is een console-vervolg op de v2.9-UX (Documentenhierarchie, echte wachttaak-badges, Accounts-kamer; PR #25 is gemerged); duurzame immutable opslag wordt niet overgeslagen; de G2-locator blijft de publicatieblocker. Protocol v2.11 is een begrensde supersessie van v2.7 uitsluitend voor de zin dat ingest MUST een URL accepteren voor officiële bestanden zonder HTML van PDF te onderscheiden: first-wave HTML MUST een geüploade freeze-file zijn (exacte bytes); live URL-HTML MUST bij ingest worden geweigerd; URL-ingest van een PDF MAG blijven; file-upload HTML/PDF en onmiddellijke byte-freeze van een geüpload bestand blijven verplicht. HTML wordt niet geheel verboden. Kennisobjecten MUST genoeg broncontext dragen om terug te keren naar de exacte plaats in dat gehashte origineel (`provenance.source_fragments`; PDF `page_bbox`; HTML `web_line_range` tegen freeze-bytes die nooit worden gereserialiseerd). De Product API MUST NOT `supported` teruggeven als de source locator van het object ontbreekt of leeg is; fail-closed; abstain (cataloguszin, geen LLM). Deze v2.11-delta implementeert die ingest-weigering en API-fail-closed niet. Protocol v2.12 is een begrensde supersessie van de lezing dat extractie objectbetekenis vastlegt, dat alleen `recommendation` `supported` MAG zijn, dat envelope `review_passes` volstaat voor publish/cutover, of dat de Product API live governance per query MAG reconstrueren: de vier lagen (bron/evidence → canonieke kennis → governance → product) blijven; Extractie MUST alleen structuur en provenance bepalen, niet de betekenis van een passage; object_type default is unclassified tot menselijke bevestiging; Operators MUST NOT types verzinnen in het MVP; unclassified is de default, geen zesde advies-type; answerability MUST vraagtype × objecttype joinen (niet alleen recommendations); alleen `recommendation` MAG als handelingsadvies terugkomen; andere typen MAGEN `supported` zijn als ze bij de vraag passen en MUST NOT advies-gewicht krijgen; publish MUST het tupel `object_id` + `object_version` + `canonical_object_hash` + `confirmed_object_type` + reviewer + decision binden, niet een envelope-vinkje; de Product API blijft een afgeleide read-only laag en MUST alleen de huidige gevalideerde gepubliceerde projectie lezen; publish/withdraw/supersede MUST die projectie atomair vervangen; een verouderde projectie na withdraw is een protocolfout. Azure/G2 blijven buiten deze delta. Waar v2.12 en v2.10 §8 botsen over welke implementatie de volgende is, geldt v2.12; het v2.10-console-vervolg blijft verplicht maar is niet de eerstvolgende implementatie na deze delta. v2.11-kernelwerk (live URL-HTML weigeren; fail-closed `supported` zonder locator) blijft verplichte wet en is geen supersessie van v2.12 §10. Protocol v2.13 is een begrensde supersessie van de lezing dat tokenbudget-chunking objectidentiteit definieert, dat fusion van condition in recommendation de default MAG zijn, dat machineclassificatie gepubliceerd type is, dat schema-v1.2-relatienamen serving-wet zijn, dat onbevestigde relaties binden, of dat `docs/extraction_rules_v0.1.md` serving-taxonomie blijft: één kennisobject MUST één bevestigbare betekeniseenheid zijn; context MUST in gereviewde relaties leven, niet in een blob; de canonieke store MUST de enige bron van waarheid zijn; retrieval-index, embeddings en projecties MUST afgeleid en wegwerpbaar zijn, nooit omgekeerd; Extractie MUST op betekenisgrenzen splitsen, niet op tokenbudget; fusion van condition in recommendation is het verboden defaultpatroon (MUST NOT splitsen alleen wanneer splitsen één grammaticale claim zou breken); Operators MUST NOT types of relatietypes verzinnen; de gesloten relatieset is `applies_if`, `except_if`, `defines`, `explains`, `supported_by`, `supersedes`, `parent`/`child`; relaties MUST door een reviewer op de exacte objectversie worden bevestigd met het v2.12-tupel; `exception` is ALTIJD high-risk (four-eyes); four-eyes MUST wanneer confirmed type `exception` is, `risk_level` high is, of een high-risk veld aanwezig is; een reviewer MUST vanaf ieder kennisobject de exacte bronpassage kunnen openen (v2.11-locators; geen nieuw locatorschema). Waar v2.13 en v2.12 §10 botsen over welke implementatie de volgende is, geldt v2.13; de volgende implementatie is de Implementation engineer op de bestaande kernel (atomaire split, gesloten relaties, type-bevestiging, high-risk four-eyes, open-origineel); DAARNA G2/Azure. Protocol v2.14 wordt in deze delta niet geschreven. v2.11-freeze/locator en v2.12-type/projectie blijven verplichte wet. Het v2.10-console-vervolg is al in code (inclusief PR #31 UI-spelling Documentenhiërarchie) en wordt niet heropend. `docs/extraction_rules_v0.1.md` wijkt voor serving-taxonomie, objectgrootte/chunking en fusion van condition in recommendation. Alle v2.6-consoleregels blijven van kracht. Alle v2.7-bron-/API-/distributieregels blijven van kracht, behalve die begrensde HTML-URL-ingestzin. Alle v2.8-gebruikers-/hiërarchieregels blijven van kracht. Alle v2.9-UX-/huisstyleregels blijven van kracht. Alle v2.10-console-/nav-/accountsregels blijven van kracht. Alle v2.11-freeze-/locatorregels blijven van kracht. Alle v2.12-type-/review-/projectieregels blijven van kracht, behalve de in v2.13 begrensde extractiegrootte-, fusion-, classificatie-, relatie- en high-risk-lezingen. Alle v2.13-object-/relatie-/four-eyesregels blijven van kracht. Protocol v2.15 is een begrensde supersessie van de 2026-08-29-lezing dat de volgende console-implementatie alleen de tweecoloms reviewkaart is (die kaart is al in code) en van iedere lezing dat het ingest-datumveld vrije tekst MAG zijn, dat de datum MAG defaulten naar vandaag of het ingest-kliktijdstip, dat display-locale in opgeslagen bytes MAG lekken, dat het versieveld vrije tekst MAG zijn, dat een jaartal als versie MAG, dat onderzoekers een aparte «zwaar/licht»- of «snel/langzaam»-schakelaar krijgen, dat extractie echte bronkoppen als `unclassified` MAG laten landen, dat de reviewlijst de typenaam (`unclassified`) als rijtitel MAG gebruiken, dat vierduizend unclassified-kaarten op één richtlijn als werklast MAGEN worden geaccepteerd, of dat extractie gewone tekst automatisch tot `recommendation` MAG promoveren: het ingest-datumveld MUST een kalenderdatumkiezer zijn; op scherm MUST de onderzoeker Europe/Amsterdam-kalendervolgorde `DD-MM-YYYY` zien en kiezen; intern MUST kernel/store/API/hash-envelope dezelfde dag als ISO `YYYY-MM-DD` bewaren (geen tijd, geen tijdzone); de datum MUST de datum op de freeze zijn (colofon / publicatiedatum); MUST NOT defaulten naar vandaag; MUST NOT het ingest-kliktijdstip zijn; leeg MUST NOT bij ingest worden geaccepteerd; geen datum = geen herleidbare bron; display-locale MUST NOT in opgeslagen bytes lekken; een EPD MUST ISO ontvangen; `01-02-2026` op scherm is 1 februari 2026, opgeslagen `2026-02-01`. Het versieveld MUST NOT vrije tekst zijn; toegestaan patroon is één of meer niet-negatieve gehele getallen gescheiden door punten (conceptueel `^[0-9]+(\.[0-9]+)*$`); MUST accepteren: `1`, `1.0`, `2.13`, `1.2.3`; MUST weigeren: `v`-prefix, letters, komma, voor-/naloopende punt, `-beta`/`-rc`, spaties, jaartal-als-versie (`2024`); leeg MUST NOT bij ingest; de machine valideert, de onderzoeker vult; geen extra UI-controle «zwaar/licht» op dit veld. Deze twee velden zijn document-/bronmetadata van de freeze, niet knowledge-object `object_version` en niet v2.14 `valid_from`/`valid_until`; Operators MUST NOT ze in recommendation-conditionvelden fuseren; invullen is geen publicatie. Er MUST NOT een aparte onderzoekercontrole «zwaar/licht» of «snel/langzaam» zijn; bevestigd (of, alleen voor wachtrij-routing, voorgesteld) `object_type` bepaalt de reviewbaan; snelle baan: `heading` (echte bronkoppen / TOC / structurele kruimels; batch-accepteren als structuur, niet als advies; headings MUST NOT als handelingsadvies worden geserveerd); trage baan: `definition`, `explanation`, `condition`, `exception`, `recommendation` en `unclassified` tot een mens een type bevestigt; Extractie MUST `heading` voorstellen voor echte bronkoppen / TOC / structurele kruimels zodat die niet allemaal als `unclassified` landen (v2.12 staat heading al toe; v2.15 maakt dat een reviewbaan-voorwaarde); al het overige MUST als `unclassified` starten en is dus traag tot menselijke typebevestiging; Extractie MUST NOT gewone tekst automatisch tot `recommendation` promoveren; de reviewlijst MUST NOT de typenaam (`unclassified`) als rijtitel gebruiken; iedere rij MUST een bronpassage-snippet (freeze-tekst van dat object) tonen; het numerieke object-id MAG secundair blijven; de snelle baan MUST batch-bevestiging van voorgestelde headings als structuur ondersteunen zodat een onderzoeker geen 4000 kaarten opent; de trage baan blijft één object (type + relaties + high-risk four-eyes); vierduizend unclassified-kaarten op één richtlijn is een fail van dit reviewoppervlak, geen te accepteren werklast; MUST NOT pagina/alinea-types verzinnen; MUST NOT bestaande gehashte objecten stilzwijgend hersplitsen (identity van al gehashte objecten blijft; nieuwe ingest na heading-voorstel MAG); bestaande Continentie-bytes MUST NOT in dit protocol opnieuw worden geëxtraheerd; een mens MUST kunnen herclassificeren (een heading die advies is wordt `recommendation` en MUST dan traag); degradatie naar snel MUST alleen door type `heading` te bevestigen, nooit via een snelheidsschakelaar; machineclassificatie blijft een voorstel, nooit gepubliceerde waarheid (v2.12/v2.13 ongewijzigd); high-risk four-eyes blijft ongewijzigd; snelle heading-acceptatie MUST NOT four-eyes omzeilen als het object high-risk is of naar een high-risk type wordt herclassificeerd; de machine MUST NOT beslissen dat iets «licht genoeg is om te serveren»; serving vereist nog het v2.12-tupel + gepubliceerde projectie + G2-locator; deze delta maakt G2 niet PASS en implementeert `publish()` niet; MUST NOT nieuwe objecttypes voor pagina/alinea toevoegen. Waar v2.15 en de 2026-08-29-lock botsen over welke implementatie de volgende is, geldt v2.15; de volgende implementatie is de Implementation engineer op de bestaande kernel/console (ingest-datumkalender + ISO-opslag + verplicht; ingest-versie dotted-integer + verplicht; extract MUST `heading` voorstellen voor echte bronkoppen; reviewlijst MUST bronpassage-snippet tonen, MUST NOT typenaam als titel; reviewwachtrijen/UI gerouteerd op type met snelle batch-bevestiging van voorgestelde headings); DAARNA G2/Azure. Protocol v2.14 wordt in deze delta niet geschreven en is niet de volgende stap. v2.11-freeze/locator, v2.12-type/projectie en v2.13-atomaire-objecten/relaties/four-eyes blijven verplichte wet en worden niet heropend. Alle v2.15-ingest-/reviewbaanregels blijven van kracht, behalve de in v2.16 begrensde heading-voorstel- (DOEN/OVERWEEG/NIET DOEN MUST NOT als heading worden voorgesteld), tiny-object-, stamp-, één-deur-, compacte-rij-, volgende-implementatie- en unpublished-Continentie-re-extract-lezingen. Protocol v2.16 is een begrensde supersessie van de v2.15-lezing dat de volgende console-implementatie alleen ingest-datum/versie/heading-banen is (die code staat op main), van iedere lezing dat Openen plus Reviewen twee deuren MAGEN blijven die beide naar objectlijsten leiden, dat DOEN/OVERWEEG/NIET DOEN rijen in Koppen of standalone kennisobjecten MAGEN zijn, dat extractie `heading` MAG voorstellen voor die stempels, dat extractie number-only/stamp-only/truncated-sentence objecten MAG emitteren, dat de reviewlijst status/checkbox/tekst over de viewport MAG spreiden, dat de typenaam of een kernel-document-id de rijtitel MAG zijn, dat een pagina van duizenden identieke unclassified-titels als werklast MAG worden geaccepteerd, of dat unpublished Continentie-bytes MUST NOT opnieuw worden geëxtraheerd: de Review-pagina MUST onderzoekers overtuigen; als zij die opent en die niet volstaat, is het project mislukt — ook als de kernel fail-closed is; twee frustraties MUST NOT blijven (onduidelijk: twee deuren, rijen over de viewport, DOEN/OVERWEEG zonder uitleg; te kleine objecten: één woord, één getal, halve zin); binnen één scherm MUST de onderzoeker weten welk document dit is, wat nu te doen is, en waarom (dit wordt wat een EPD MAG zeggen); UI-copy MUST onderzoekerstaal zijn; primaire actie MUST visueel overduidelijk zijn; kernel-ids MUST NOT de rijtitel zijn; MUST NOT twee deuren Openen plus Reviewen behouden die beide naar objectlijsten leiden; één documentkaart, één primaire knop Beoordeel; twee benoemde stacks met counts: Koppen (echte TOC/sectietitels van de freeze; batch-bevestigen als structuur, nooit als advies; headings MUST NOT als handelingsadvies) en Inhoud (definition, explanation, condition, exception, recommendation en unclassified tot getypt; één-objectkaart; high-risk four-eyes ongewijzigd); MUST NOT een pagina van duizenden identieke unclassified-titels; MUST NOT onderzoekercontrole «zwaar/licht»; compacte rijen: één regel freeze-bronzin (of echte koptekst) plus korte status; MUST NOT status/checkbox/tekst over de viewport spreiden; MUST NOT typenaam of kernel-document-id als titel; DOEN/OVERWEEG/NIET DOEN zijn stempels, geen objecten; MUST NOT GRADE-jargon; MUST NOT nieuw objecttype; MUST NOT rijen in Koppen; MUST NOT standalone kennisobjecten; voorkeurslock: stempel op de volgende recommendation; reviewkaart toont één zin in onderzoekerstaal, bijv. «Sterkte van de aanbeveling: DOEN — dit moet de zorgverlener doen.»; gesloten waarden op type `recommendation` alleen: `doen` | `overweeg` | `niet_doen`; dit is GEEN fusion van condition in recommendation; de mens bevestigt sterkte samen met de advieszin; Extractie MUST NOT `heading` voorstellen voor DOEN/OVERWEEG/NIET DOEN ook als de freeze ze in heading-tags wikkelt; Extractie MUST NOT tiny objects emitteren (alleen lijstnummer `1.`, alleen stempel, zinsfragment dat niet als één betekeniseenheid kan staan; een los naloopwoord van de vorige zin is verboden); freeze-bronbytes en SHA-256 blijven; bestaande gepubliceerde objecten worden niet stilzwijgend herschreven (er is geen gepubliceerde projectie); Continentie is unpublished: een nieuwe extract van dezelfde freeze-bytes is VERPLICHT zodat de reviewpagina deze bar kan halen; source-hash blijft; unpublished objectidentiteiten MAGEN door die nieuwe extract worden vervangen; MUST NOT in de UI liegen door opgeslagen fragmenten te verbergen zonder nieuwe extract. Serving/G2 ongewijzigd: alleen bevestigde `recommendation` MAG `supported` / handelingsadvies; de machine MUST NOT beslissen dat iets licht genoeg is om te serveren; four-eyes ongewijzigd; G2 blijft de publicatieblocker. Waar v2.16 en v2.15 botsen over heading-voorstel voor stempels, tiny objects, unpublished Continentie-re-extract, één deur, compacte rijen of welke implementatie de volgende is, geldt v2.16; de volgende implementatie is de Implementation engineer op de bestaande kernel/console (één deur Beoordeel; twee stacks Koppen/Inhoud met counts; compacte brontekst-rijen; stempels op recommendation; extract MUST NOT heading voorstellen voor DOEN/OVERWEEG/NIET DOEN; extract MUST NOT tiny objects emitteren; nieuwe extract van unpublished Continentie); DAARNA William click-through van de draaiende console (geen screenshots); DAARNA Azure ZIP van die main vanaf een V&VN-vertrouwd apparaat; DAARNA G2. Protocol v2.14 wordt in deze delta niet geschreven en is niet de volgende stap. v2.11-freeze/locator, v2.12-type/projectie, v2.13-atomaire-objecten/relaties/four-eyes en v2.15-ingest-datum/versie/typebanen blijven verplichte wet, behalve de hier begrensde supersessies. Overige eisen blijven fail-closed. Alle v2.16-reviewpaginaregels blijven van kracht, behalve de in v2.17 begrensde slogan-, HELP_ONCE-via-negativa-, prefilled-Onderwerp-, raw-HTML-bronpassage-, site-chrome-object-, stempel-UI-op-niet-recommendation-, uitgerekte-relatiecheckbox- en volgende-implementatie-lezingen. Protocol v2.17 is een begrensde supersessie van de v2.16-lezingen die (1) de EPD-MAG-slogan als Review-leadcopy eisten of toestonden (de gehele zin «Dit wordt wat een EPD MAG zeggen.» MAG weg; niet verzwakken), (2) HELP_ONCE-via-negativa op onderzoekerskamers MAGEN, (3) een vooringevuld Onderwerp MAGEN, (4) bronpassage raw HTML-freeze-slices als onderzoeker-rechterkolom MAGEN, ook als één-kaart-uitzondering, (5) site-chrome als heading/unclassified-objecten MAGEN, inclusief één-woord Tools, (6) **Sterkte van de aanbeveling** / DOEN/OVERWEEG/NIET DOEN-picker op een niet-`recommendation`-object MAGEN, of (7) relatiecheckbox en doeltitel naar tegenoverliggende randen van de viewport MAGEN spreiden: UI-copy MUST onderzoekerstaal zijn, geen slogans, geen «wat een EPD MAG zeggen», geen claim van één abonnee-klasse; de gehele zin «Dit wordt wat een EPD MAG zeggen.» MAG weg (niet verzwakken); leadcopy zegt wat te doen op dit scherm (Beoordeel Koppen als structuur, Inhoud als kennisobjecten) zonder marketing; via-negativa MUST NOT op onderzoekerspagina's, ook niet als ingeklapte help «Over deze console»; het ingest-veld Onderwerp/familie MUST leeg zijn op een verse nieuwe ingest (geen vooringevulde `continentie`; de onderzoeker vult; klasse blijft buiten deze delta); de bronpassage-rechterkolom MUST dezelfde leesbare zin tonen als het kennisobject, zonder HTML-tags, CSS-klassenamen of kennisplatform-markup, op ieder object / de hele freeze (geen één-kaart-uitzondering; opgenomen antwoord op «Is dat hoe het definitief eruit gaat zien?» is NEE); v2.11-freezebytes en locators blijven exact (MUST NOT herserialiseren/opnieuw opslaan); het onderzoekersoppervlak is zichtbaar proza afgeleid van die locator, niet de ruwe tagsoup; Open-origineel blijft verplicht vóór typebevestiging; Extractie MUST NOT kennisplatform-chrome (Home, Richtlijnen, Meedenken, Kennisinstituut V&VN, Tools, Veelgestelde vragen, duplicaten) als kennisobjecten of Koppen emitteren, ook niet als één-woord object; Tools/Home/Richtlijnen/Meedenken MUST NOT objecten zijn; Koppen = echte richtlijn-TOC/sectietitels van het richtlijnlichaam, geen sitenav; stempel-UI (**Sterkte van de aanbeveling**, DOEN/OVERWEEG/NIET DOEN-picker) MUST NOT verschijnen behalve op type `recommendation`; een nav-woord MUST NOT een aanbevelingssterkte-controle krijgen; de relatielabel MUST direct naast het vinkje zitten; MUST NOT checkbox en doeltitel naar tegenoverliggende randen van de viewport spreiden; deze regels gelden voor de hele freeze / ieder object / iedere sectie (Inleiding, Doel, Doelgroep, Aanleiding zijn voorbeelden, geen gesloten lijst); 2641 unclassified op één richtlijn blijft een fail van het reviewoppervlak (v2.15/v2.16-bar ongewijzigd). Serving/G2 ongewijzigd: alleen bevestigde `recommendation` MAG `supported` / handelingsadvies; de machine MUST NOT beslissen dat iets licht genoeg is om te serveren; four-eyes ongewijzigd; G2 blijft de publicatieblocker; `publish()` blijft G2-BLOCKED; Capture is geen publicatie. Unpublished Continentie MAG na deze wet opnieuw worden geëxtraheerd; source SHA-256 blijft; unpublished objectidentiteiten MAGEN worden vervangen; MUST NOT opgeslagen fragmenten verbergen zonder nieuwe extract. Waar v2.17 en v2.16 botsen over slogan-copy, via-negativa-help, vooringevuld Onderwerp, raw-HTML-bronpassage, site-chrome-objecten, stempel-UI-op-niet-recommendation, uitgerekte-relatiecheckboxes of welke implementatie de volgende is, geldt v2.17; die v2.17-golf is in code op `main` `3e811bf0fc9f`. Protocol v2.14 wordt in deze delta niet geschreven en is niet de volgende stap. v2.11-freeze/locator, v2.12-type/projectie, v2.13-atomaire-objecten/relaties/four-eyes, v2.15-ingest-datum/versie/typebanen, v2.16-één-deur/stacks/rijen/stempels/tiny-objects en v2.17-onderzoekersoppervlak blijven verplichte wet, behalve de hier begrensde supersessies. Alle v2.17-onderzoekersoppervlakregels blijven van kracht, behalve de in v2.18 begrensde dubbele-kaartzin-, truncated-sentence-split-, identieke-clean_text- en volgende-implementatie-lezingen. Protocol v2.18 is een begrensde supersessie van iedere lezing dat (1) de reviewkaart dezelfde freeze-zin als heading én body MAG herhalen, inclusief de lezing dat de v2.16-compacte-rij-bar alleen de lijst bindt en niet de open kaart; (2) extractie een grammaticale voortzetting / naloopzin tot een nieuw object MAG splitsen, inclusief de lezing dat het v2.16 truncated-sentence / tiny-object-verbod al is gehaald door de v2.17 Continentie-re-extract (die re-extract faalde het nog); of (3) extractie identieke clean_text-passages als meerdere objecten MAG emitteren omdat de freeze-HTML ze herhaalt (samenvatting versus module): de open reviewkaart MUST de freeze-zin één keer tonen; MUST NOT dupliceren als zowel h3/titel als body; compacte rij/kaart: één bronzin plus korte status; de v2.16-compacte-rij-bar geldt voor de open kaart, niet alleen de lijst; Extractie MUST NOT een grammaticale voortzetting van de vorige zin tot een nieuw object splitsen; naloopzinnen (bijv. «Eventueel met hulp van de mantelzorger.», «Bijvoorbeeld …») MUST in hetzelfde kennisobject blijven als de zin die zij afmaken; dit herhaalt en verscherpt het v2.16 truncated-sentence / tiny-object-verbod; Extractie MUST NOT een tweede kennisobject emitteren waarvan de zichtbare freeze-proza (clean_text) identiek is aan een object dat al uit deze freeze is geëmitteerd; herhaalde HTML is geen extra kennis; onderscheiden echte koppen in verschillende secties MAGEN blijven («1.1 Inleiding» vs «2. Inleiding» zijn geen identieke strings); MUST NOT opgeslagen fragmenten verbergen zonder nieuwe extract; unpublished Continentie MAG opnieuw worden geëxtraheerd; source SHA-256 blijft; unpublished identiteiten MAGEN worden vervangen. Chrome Tools/Home is weg (v2.17 gehouden). Slogan is weg (v2.17 gehouden). Deze fail is duplicatie + truncated-sentence-split. Serving/G2/four-eyes/v2.14/Azure ongewijzigd: alleen bevestigde `recommendation` MAG `supported` / handelingsadvies; de machine MUST NOT beslissen dat iets licht genoeg is om te serveren; four-eyes ongewijzigd; G2 blijft de publicatieblocker; `publish()` blijft G2-BLOCKED; Capture is geen publicatie; Protocol v2.14 is niet dit bestand en is niet de volgende stap. Waar v2.18 en v2.17 botsen over dubbele kaartzin, truncated-sentence-split, identieke clean_text of welke implementatie de volgende is, geldt v2.18; die v2.18-golf is in code op `main` `4ebfdbb88cdb`. Protocol v2.14 wordt in deze delta niet geschreven en is niet de volgende stap. v2.11-freeze/locator, v2.12-type/projectie, v2.13-atomaire-objecten/relaties/four-eyes, v2.15-ingest-datum/versie/typebanen, v2.16-één-deur/stacks/rijen/stempels/tiny-objects, v2.17-onderzoekersoppervlak en v2.18-reviewkaart-één-keer/extract-dedup blijven verplichte wet, behalve de hier begrensde supersessies. Alle v2.18-reviewkaart-/extract-dedupregels blijven van kracht, behalve de in v2.19 begrensde trage-baan-unclassified-één-object-plicht-, 2008-Inhoud-werklast- en volgende-implementatie-lezingen. Protocol v2.19 is een begrensde supersessie van iedere lezing dat ieder unclassified-object MUST een trage-baan één-objectkaart zijn die de onderzoeker MUST openen, of dat 2008 Inhoud-kaarten een te accepteren werklast is: onderzoekers MUST NOT verplicht worden 2008 Inhoud-kaarten één voor één te openen; dat is dezelfde fail als 4000 unclassified: vermoeidheid, geen assurance; live bewijs: Inhoud (2008) / Koppen 78 op Continentie na de v2.18-extract (`main` `4ebfdbb88cdb`, snapshot `snap-ac59cf24f946088e-e402c4d3`); eigenaar zei dat het er beter uitziet, vroeg daarna of onderzoekers die 2008 objecten met de hand moeten doen, daarna «zet dat in protocol.»; Koppen MAGEN en MUSTEN batch-bevestigbaar blijven als structuur, nooit als advies (v2.15/v2.16 ongewijzigd); de onderzoeker-verplichte trage review op een freeze is voorgestelde `recommendation`, plus `condition` / `exception` / ieder high-risk object (four-eyes ongewijzigd); die begrenzen het advies; dat MUSTEN zij met de hand doen; resterende `unclassified` MUST NOT als gelijke één-voor-één-plicht van duizenden kaarten worden gepresenteerd; unclassified wordt nooit geserveerd (`supported` / handelingsadvies), dus 2000 klikken daarop voegen geen assurance toe; serving blijft fail-closed: alleen bevestigde `recommendation` MAG `supported` / handelingsadvies; de machine MUST NOT beslissen dat iets licht genoeg is om te serveren; MUST NOT een onderzoekercontrole «zwaar/licht» of «snel/langzaam» (v2.15 ongewijzigd); machineclassificatie blijft een voorstel, nooit gepubliceerd type; MUST NOT types auto-bevestigen; MUST NOT gewone tekst automatisch tot recommendation promoveren; definition/explanation MAGEN bestaan en MAGEN later niet-adviesvragen beantwoorden eens bevestigd; zij zijn NIET de MVP-onderzoeker-2000-kaarten-plicht voor handelingsadvies; extract SHOULD nog grover worden (kennisplatform-HTML is te praatgraag) maar de bar van deze delta is reviewPLICHT en wachtrijpresentatie, geen nieuw objecttype; v2.16 tiny-objects, v2.17 chrome, v2.18 geen-dubbele-zin / naloopzin / identieke clean_text blijven van kracht; 2008 unclassified/Inhoud-kaarten op één richtlijn blijft een fail van dit reviewoppervlak (zelfde bar als v2.15/v2.16); unpublished Continentie MAG na deze wet opnieuw worden geëxtraheerd; source SHA-256 blijft; unpublished identiteiten MAGEN worden vervangen; MUST NOT opgeslagen fragmenten verbergen zonder nieuwe extract. Serving/G2/four-eyes/v2.14/Azure ongewijzigd: alleen bevestigde `recommendation` MAG `supported` / handelingsadvies; de machine MUST NOT beslissen dat iets licht genoeg is om te serveren; four-eyes ongewijzigd; G2 blijft de publicatieblocker; `publish()` blijft G2-BLOCKED; Capture is geen publicatie; Protocol v2.14 is niet dit bestand en is niet de volgende stap. Waar v2.19 en v2.18 botsen over trage-baan-unclassified-één-object-plicht, 2008-Inhoud-werklast of welke implementatie de volgende is, geldt v2.19; die v2.19-golf is in code op `main` `9987a976d719`. Protocol v2.14 wordt in deze delta niet geschreven en is niet de volgende stap. v2.11-freeze/locator, v2.12-type/projectie, v2.13-atomaire-objecten/relaties/four-eyes, v2.15-ingest-datum/versie/typebanen, v2.16-één-deur/stacks/rijen/stempels/tiny-objects, v2.17-onderzoekersoppervlak, v2.18-reviewkaart-één-keer/extract-dedup en v2.19-reviewplicht/wachtrij blijven verplichte wet, behalve de hier begrensde supersessies. Alle v2.19-reviewplicht-/wachtrijregels blijven van kracht, behalve de in v2.20 begrensde Continentie-as-productidentiteit-, leftover-unpublished-must-stay-, SSH-wipe-as-productpad- en volgende-implementatie-lezingen. Protocol v2.20 is een begrensde supersessie van iedere lezing dat PROTOCOL.md Continentie-only wet is, dat de volgende freeze Continentie MUST zijn, dat unpublished capture MUST op de console blijven tot publicatie, dat SSH/wipe van /home/data het productpad is, of dat four-eyes / een tweede benoemde reviewer vereist is om unpublished capture te verwijderen: PROTOCOL.md is de wet van V&VN Data Services voor iedere richtlijn, niet Continentie-only; Continentie verschijnt in v2.16–v2.19 als live bewijs van fails (stempels als Koppen, 2008 Inhoud-kaarten), niet als productidentiteit; die historische bewijszinnen MUST blijven en MUST NOT worden gestript; de volgende freeze MUST NOT Continentie hoeven zijn; unpublished captured snapshots MAGEN van de operations console worden verwijderd door een geautoriseerde console-operator (zelfde klasse als ingest: benoemd researcher/reviewer-account, geen geheim engineer-pad); dit is eigenaarsgeautoriseerde opruiming van unpublished capture, geen publicatie, geen G2, geen verbergen van fragmenten van een freeze die in Review blijft; MUST een echte verwijdercontrole op de documentkaart / Review-chooser voor unpublished snapshots alleen; label in onderzoekerstaal (bijv. Verwijder unpublished document); MUST bevestigen vóór uitvoering (destructief); na delete MUST de snapshot NOT verschijnen op Inleveren/Review/Documentenhierarchie-lijsten; opgeslagen objecten+envelope voor die snapshot_id zijn weg; freeze-bytes van die unpublished bron MAGEN mee worden verwijderd; MUST NOT andere snapshots of `/home/data` globaal raken; MUST een audit-ledgerrij toevoegen (wie, wanneer, snapshot_id, bron-SHA-256, titel); Capture blijft geen publicatie; MUST NOT een gepubliceerde projectie of iets dat is gepubliceerd verwijderen; `publish()` blijft G2-BLOCKED; er is geen gepubliceerde Continentie; MUST NOT dit gebruiken om geselecteerde objecten binnen een freeze die in review blijft te verbergen (v2.16 hide-fragments-without-extract blijft); alleen het hele unpublished snapshot; MUST NOT SSH/wipe van `/home/data` als productpad; de console-actie is het pad; four-eyes is niet vereist om unpublished capture te verwijderen; de uploader MAG unpublished die zij captureden verwijderen; een tweede benoemde reviewer is niet vereist voor delete (delete is geen type-bevestiging). Serving/G2/four-eyes/v2.14/Azure ongewijzigd: alleen bevestigde `recommendation` MAG `supported` / handelingsadvies; de machine MUST NOT beslissen dat iets licht genoeg is om te serveren; four-eyes ongewijzigd voor type-bevestiging en high-risk; four-eyes is niet vereist om unpublished capture te verwijderen; G2 blijft de publicatieblocker; `publish()` blijft G2-BLOCKED; Capture is geen publicatie; Protocol v2.14 is niet dit bestand en is niet de volgende stap. Waar v2.20 en v2.19 botsen over Continentie-as-productidentiteit, unpublished-snapshot-delete of welke implementatie de volgende is, geldt v2.20; die unpublished-delete-golf is in code op `main` `ba3c85cec8e100e289e25e6a33fbf9440676c26e`. Alle v2.20-iedere-richtlijn-/unpublished-delete-regels blijven van kracht, behalve de in v2.21 begrensde volgende-implementatie- en Azure-ZIP-van-v2.20-nu-lezingen. Protocol v2.21 is een begrensde supersessie van iedere lezing dat de volgende implementatie Azure ZIP van v2.20, Cloud Shell of G2-activatie is, dat G2-status van een stale static JSON-veld MAG afhangen, of dat PR #82 MAG worden geactiveerd voordat de vier fouten zijn hersteld ÉN een Azure test-app bestaat: eigenaar pauzeerde Cloud Shell en lockte een vier-golvenprogramma voor gecontroleerd gebruik met echte richtlijnbronnen; prioriteit is bronintegriteit, heldere kennisobjecten, veilige omgevingsscheiding, herstelbaarheid; volgorde MUST A daarna B daarna C daarna D; Cloud Shell / production ZIP blijven uit tot golf A op een gecontroleerde SHA staat; deze delta MAPT bestaande wet, herschrijft die niet; Protocol v2.16–v2.18 verbieden al tiny objects, stempel-als-kop/object, naloopzinnen, chrome-objecten en identieke clean_text; Protocol v2.19 is duty-queue; Protocol v2.20 unpublished-delete staat al op main `ba3c85cec8e100e289e25e6a33fbf9440676c26e` en is GEEN vijfde golf; PROTOCOL.md is wet voor iedere richtlijn, niet Continentie-only (v2.20); Continentie-bewijszinnen in v2.16–v2.19 MUST blijven; G2-readiness (PR #69) pinte al azure-identity/azure-storage-blob en een report-only preflight; G2 blijft BLOCKED; `publish()` blijft G2-BLOCKED; RBAC Storage Blob Data Contributor op aidataservice/canonical-sources voor vvn-metis-console is extern; PR #82 is OPEN en MUST NOT worden geactiveerd tot de vier fouten zijn hersteld ÉN een Azure test-app bestaat; golf A MUST een context-bewuste splitter plus een toetsbare reject-functie implementeren; een inhoudelijk kennisobject MUST één complete, zelfstandig leesbare betekeniseenheid zijn; MUST bronpassage+locator naar de freeze hebben; MUST NOT alleen een nummer, label, kopwoord, nav, stempel of zinsfragment zijn; MUST NOT identieke clean_text uit dezelfde freeze dupliceren; echte bronkoppen MAGEN als heading bestaan, alleen structuur, nooit advies, geen recommendation-stempel, batch-bevestigbaar als structuur; DOEN/OVERWEEG/NIET DOEN zijn geen objecten en geen Koppen; zij zijn een eigenschap van een volledige recommendation samen met de advieszin; splitter MUST naloop-/afhankelijke zinnen aan de vorige betekenisvolle zin hechten; MUST een stempel aan de onmiddellijk volgende advieszin hechten; MUST chrome/nav/lijstnummers/losse labels/leeg/te-kort FILTEREN VÓÓR objectaanmaak; MUST dubbele clean_text in dezelfde snapshot voorkomen; MUST freeze-bytes en locators exact houden (alleen afgeleide extract); reject-functie MUST weigeren: geen zelfstandige betekenis; onder een gedocumenteerde minimum-betekenisdrempel; alleen stempel/nummer/nav; grammaticale voortzetting van de vorige; identiek aan een eerder object uit dezelfde freeze; uitzonderingen MUST expliciet en getest zijn; korte echte definities en officiële koppen MUST NOT worden weggelaten; MUST NOT «Inleiding» als chrome behandelen; Home/Tools/Richtlijnen/Meedenken zijn chrome; Inleiding als echte sectietitel MAG heading blijven; regressiefixtures MUST van echte Continentie-failpatronen komen; acceptatie: geen van die failpatronen landen als standalone inhoudelijke objecten in de review-plichtwachtrij; geen infrastructuur in golf A; golf B G2-status MUST NOT van een stale static JSON-veld afhangen; read-only preflight MUST Blob-container bereikbaar, MI bruikbaar, vereiste Blob-rol aanwezig of toegang daadwerkelijk bewezen, container matcht actieve omgeving, bron kan byte-identiek worden opgeslagen en teruggelezen; gecontroleerde SHA-256-smoke; machineleesbaar bewijs (timestamp, environment, container, SHA-256, outcome); G2 PASS alleen na geslaagde gecontroleerde test, anders BLOCKED; de publicatiegate MUST NOT openen omdat een app-setting aanwezig is; deze protocol claimt geen G2 PASS; RBAC-grant blijft extern; golf C MUST het packaging-script via bash aanroepen of executable maken; Azure ZIP MUST dependencies bouwen of het artefact MUST volledig deploybaar zijn; git-archive-only is niet genoeg; live Oryx-during-deploy veroorzaakte HTTP_504 op B1; storage-account/container per omgeving via veilige app-settings, geen secrets in Git; gescheiden deploy-identiteiten MUST zo scoped zijn dat test niet production kan en production niet test; production is handmatig (alleen een volledige SHA al op main, na protection/approval); MUST NOT runtime-data uit Git deployen; MUST NOT `/home/data` overschrijven; merge naar main MAG alleen naar test deployen; production vereist een expliciete release-actie van exact dezelfde geteste SHA; de vier bekende PR #82-fouten MUST worden hersteld; MUST NOT een test-App Service in deze protocol-PR aanmaken of activeren; MUST NOT Azure starten in golven A of B; golf D MUST `/home/data/metis-console` inventariseren (accounts/rollen, documentsnapshots, reviewbesluiten en audit-ledger, canonieke objecten, afgeleide projecties); golf D MUST export/backup, gecontroleerde restore naar een schone omgeving, integriteitscheck na restore, en een test die bewijst dat een deployment bestaande runtime-data niet verwijdert; geen grote databasemigratie; MUST de migratiegrens documenteren (een managed database wordt vereist vóór meerdere App Service-instances of gelijktijdige multi-reviewer-writes). Serving/G2/four-eyes/v2.14/Azure ongewijzigd: alleen bevestigde `recommendation` MAG `supported` / handelingsadvies; de machine MUST NOT beslissen dat iets licht genoeg is om te serveren; four-eyes ongewijzigd voor type-bevestiging en high-risk; G2 blijft de publicatieblocker; `publish()` blijft G2-BLOCKED; Capture is geen publicatie; Protocol v2.14 is niet dit bestand en is niet de volgende stap; Azure is niet deze protocol-PR. Waar v2.21 en v2.20 botsen over welke implementatie de volgende is, of over Azure-ZIP-van-v2.20-nu, geldt v2.21; de volgende implementatie is de Implementation engineer op de bestaande kernel/extract voor precies golf A only (context-bewuste splitter + toetsbare reject-functie + Continentie-regressiefixtures); DAARNA B, DAARNA C, DAARNA D. Niet Azure ZIP van v2.20 tot A op een gecontroleerde SHA staat, tenzij de eigenaar Cloud Shell heropent. Protocol v2.14 wordt in deze delta niet geschreven en is niet de volgende stap. v2.11-freeze/locator, v2.12-type/projectie, v2.13-atomaire-objecten/relaties/four-eyes, v2.15-ingest-datum/versie/typebanen, v2.16-één-deur/stacks/rijen/stempels/tiny-objects, v2.17-onderzoekersoppervlak, v2.18-reviewkaart-één-keer/extract-dedup, v2.19-reviewplicht/wachtrij en v2.20-iedere-richtlijn-wet/unpublished-delete blijven verplichte wet, behalve de hier begrensde supersessies. Alle v2.21-golfdefinities blijven van kracht, behalve de in v2.22 begrensde volgende-implementatievolgorde A daarna B daarna C daarna D. Protocol v2.22 is een begrensde supersessie van Protocol v2.21 §3-volgorde A daarna B daarna C daarna D, alleen voor volgende-implementatievolgorde: golven zelf ongewijzigd; historische v2.21-golfdefinities blijven wet; golf A staat al in code op `main` `512ffa5026d06ff804434ddf4d07a08a36c02305`; volgende implementatie MUST golf C daarna golf D; MUST NOT deploy-test/deploy-production activeren tot Azure test-App Service `vvn-metis-console-test` bestaat; merge naar `main` MUST NOT automatisch deployen naar een ontbrekende test-app; ZIP MUST volledig deploybaar zijn inclusief dependencies; git-archive-only is niet genoeg; per-omgeving opslag via veilige app-settings, geen secrets in Git; gescheiden test- versus production-deployidentiteiten; `--clean true` wist wwwroot en MUST NOT runtime-data verwijderen; DAARNA één Cloud Shell / production ZIP van die gecontroleerde SHA (A+C+D); geen live-URL-ingest; ingest van een nieuwe freeze MUST NA die ZIP, niet ervoor; live draait nog pre-golf-A extract tot ZIP; golf B (G2-bewijs/smoke) NA die ZIP; G2 blijft BLOCKED; `publish()` blijft G2-BLOCKED; ZIP opent publicatie niet; claimt geen G2 PASS; MUST NOT Azure test-app starten in deze protocol-PR; MUST NOT fragmenten verbergen; MUST NOT SSH-wipe van `/home/data`; MUST NOT v2.16–v2.21-bestanden herschrijven behalve index-/conflictpointers; MUST NOT C/D in deze PR implementeren; v2.20 unpublished-delete blijft op main, GEEN vijfde golf; golf C (PR #82 afmaken, niet activeren) ÉN golf D (backup/restore + deploy-persistentietest), zelfde kernel/repo, geen G2 PASS, geen Blob-grant; daarna stoppen voor William Cloud Shell ZIP van die SHA, daarna ingest. Serving/G2/four-eyes/v2.14/Azure ongewijzigd: alleen bevestigde `recommendation` MAG `supported` / handelingsadvies; de machine MUST NOT beslissen dat iets licht genoeg is om te serveren; four-eyes ongewijzigd voor type-bevestiging en high-risk; G2 blijft de publicatieblocker; `publish()` blijft G2-BLOCKED; Capture is geen publicatie; Protocol v2.14 is niet dit bestand en is niet de volgende stap; Azure is niet deze protocol-PR. Waar v2.22 en v2.21 botsen over welke implementatie de volgende is, of over de v2.21 §3-volgorde A daarna B daarna C daarna D, geldt v2.22. Alle v2.22-ZIP-dan-B-live-padregels blijven van kracht, behalve de in v2.23 begrensde lezing dat vereenvoudiging die ZIP MAG vervangen. Protocol v2.23 is een begrensde supersessie van iedere lezing dat vereenvoudiging de volgende live-stap is in plaats van William Cloud Shell ZIP van A+C+D SHA `a566af56`, dat PR #82 MAG worden gemerged, dat de eerste DELETE-snede `semantic_transform_v2.py` / `prepublication_gate_v2.py` / `validation_workflow_v2.py` / `apply_second_review.py` / `canonical_store.py` / `service_app.py` MAG verwijderen, of dat deze review GD-03 of publicatie is: eigenaar vroeg Auditor om een code-oppervlaktereview op `main` HEAD `a566af56c8c88e76cb4de7fa51642b408705da02`; Auditor-oordeel REQUEST SIMPLIFICATION; geen GD-03, geen publicatie; G2 blijft BLOCKED; deze delta MUST NOT het live-pad wijzigen; volgende live-stap blijft William Cloud Shell ZIP van A+C+D SHA `a566af56`, daarna ingest van een freeze, daarna golf B (G2-bewijs); vereenvoudiging is NA die ZIP, niet in plaats daarvan; PR #82 blijft gesloten/ongemerged; golven A, C en D staan al in code op die SHA; eerste DELETE-snede, zero-caller src/-modules only: extract_pdf.py, semantic_transform.py, validation_workflow.py, build_second_review_queue.py, pre_step5_gate.py, import_expert_validation.py, reconcile_legacy_review.py, evaluate_safe_retrieval.py, build_retrieval_document.py; MUST NOT `src/semantic_transform_v2.py` verwijderen; MUST NOT `src/prepublication_gate_v2.py` verwijderen; MUST NOT `src/validation_workflow_v2.py` verwijderen; MUST NOT `src/apply_second_review.py` verwijderen; MUST NOT `src/canonical_store.py` verwijderen; service_app.py EN product_api_v1.py blijven; twee producten, geen leftovers; live ingest gebruikt extract_pdf_v2, extract_html_v1, semantic_transform_generic_v1, prepublication_gate_v3; operations_console_v1.py versus operations_console_app.py split blijft; geen template-engine; golf A splitter + reject_candidate blijven; test_protocol_v2_* en test_v2* blijven beide; scripts/run_integrity_sprint.sh MAG in dezelfde of de volgende wijziging naar committed fixtures wijzen; MUST NOT v2/v21/generic mergen in die PR; CLI review-queue versus console review_stacks / slow_review_duty is dual live path; Console is de onderzoeker-plichtwachtrij; plan, MUST NOT stilzwijgend verwijderen; volgende implementatie NA die ZIP is één deletion-PR; bestaande pytest only; MUST NOT splitter of console in die PR raken; Implementation MUST opnieuw bewijzen dat er geen live callers zijn vóór iedere delete; HANDOFF.md MUST NOT opnieuw worden aangemaakt; MUST NOT deletion-PR in deze PR implementeren; claimt geen G2 PASS. Serving/G2/four-eyes/v2.14/Azure ongewijzigd: alleen bevestigde `recommendation` MAG `supported` / handelingsadvies; de machine MUST NOT beslissen dat iets licht genoeg is om te serveren; four-eyes ongewijzigd voor type-bevestiging en high-risk; G2 blijft de publicatieblocker; `publish()` blijft G2-BLOCKED; Capture is geen publicatie; Protocol v2.14 is niet dit bestand en is niet de volgende stap; Azure is niet deze protocol-PR. Waar v2.23 en v2.22 botsen over of vereenvoudiging de ZIP van `a566af56` MAG vervangen, of over de eerste DELETE-snede, geldt v2.23. Protocol v2.14 wordt in deze delta niet geschreven en is niet de volgende stap. v2.11-freeze/locator, v2.12-type/projectie, v2.13-atomaire-objecten/relaties/four-eyes, v2.15-ingest-datum/versie/typebanen, v2.16-één-deur/stacks/rijen/stempels/tiny-objects, v2.17-onderzoekersoppervlak, v2.18-reviewkaart-één-keer/extract-dedup, v2.19-reviewplicht/wachtrij, v2.20-iedere-richtlijn-wet/unpublished-delete, v2.21-golfdefinities en v2.22-ZIP-dan-B-live-pad blijven verplichte wet, behalve de hier begrensde supersessies. Overige eisen blijven fail-closed.

Dit bestand is de stabiele ingang naar het geldende protocol. Normatieve eisen worden uitsluitend in de versiegebonden protocolspecificatie gewijzigd. Een nieuwe productregel, architectuurgrens, safety-invariant, verantwoordelijkheid of verboden route vereist eerst een geversioneerde protocolwijziging.

## Productgrens

V&VN Data Services beheert de gevalideerde kennislaag, Product API, interne inspection en — onder Protocol v2.6 — een interne operations console als menselijk oppervlak over de knowledge kernel. De vier lagen blijven bron/evidence → canonieke kennis → governance → product. De console-MVP ingest+review bestaat in code; de v2.9-UX-rewrite (taakgericht onderzoekersoppervlak, V&VN digitale stylesheet, officieel beeldmerk) bestaat nu in code (PR #25). Protocol v2.10 eist Documentenhierarchie als blijvende boomheading (niet Familieboom; kernel blijft familie × klasse; familie blijft een haak, geen nieuw bestand), zichtbare wachttaak-badges op iedere topnav-heading met echte kernelwachtrijen (afwezig of zero-hidden als er niets wacht; Publish-badge impliceert geen G2-PASS), en een Accounts-kamer (geen chat, geen vijfde klinische kamer in plaats van ingest/review/publish) waarin een publisher gebruikers mag aanmaken (gebruikersnaam, weergavenaam, wachtwoord), rollen mag toewijzen en roltoewijzing mag wijzigen, zonder die follow-up in de v2.10-protocolwijziging te implementeren. Protocol v2.11 eist dat officiële first-wave HTML een geüploade freeze-file is (exacte bytes); live URL-HTML MUST bij ingest worden geweigerd; PDF-upload blijft in; URL-ingest van een PDF MAG blijven; URL-ingest van HTML MUST NOT; HTML wordt niet geheel verboden. Kennisobjecten MUST een source locator dragen (`provenance.source_fragments`; PDF `page_bbox`; HTML `web_line_range` tegen freeze-bytes). De Product API MUST NOT `supported` teruggeven als de source locator ontbreekt of leeg is; fail-closed; abstain (cataloguszin, geen LLM). Deze protocolwijziging implementeert die ingest-weigering en API-fail-closed niet. Protocol v2.12 eist: Extractie MUST alleen structuur en provenance bepalen, niet de betekenis; een heading MAG `object_type` heading worden; al het overige MUST defaulten naar unclassified; de machine MAG een type voorstellen; een menselijke reviewer MUST het definitieve object_type bevestigen vóór publicatie; een onbevestigd voorstel MUST NOT als gepubliceerd type worden behandeld. De gesloten object-typeset is heading, definition, explanation, condition, exception, recommendation; Operators MUST NOT types verzinnen; unclassified is de default, geen zesde advies-type. Answerability MUST vraagtype × objecttype joinen (niet alleen recommendations); alleen recommendation MAG als handelingsadvies terugkomen; definition MAG een definitievraag beantwoorden; explanation MAG uitleg ondersteunen; condition en exception MAGEN advies begrenzen; die andere typen MUST NOT advies-gewicht krijgen (geen advies-gewicht). Cutover/publish MUST NOT envelope `review_passes` alleen vertrouwen; minimumbinding is `object_id` + `object_version` + `canonical_object_hash` + `confirmed_object_type` + reviewer + decision. De Product API blijft een afgeleide read-only laag; serving MUST een gevalideerde gepubliceerde projectie gebruiken; publish, withdraw en supersede MUST die projectie atomair vervangen; een verouderde projectie is een protocolfout. Deze protocolwijziging implementeert extract/API/console niet. Azure/G2 blijven buiten deze delta. Protocol v2.13 eist: één kennisobject MUST één bevestigbare betekeniseenheid zijn; context leeft in gereviewde relaties, niet in een blob; de canonieke store is de enige bron van waarheid; retrieval-index, embeddings en projecties zijn afgeleid en wegwerpbaar, nooit omgekeerd. Extractie MUST op betekenisgrenzen splitsen, niet op tokenbudget; tokenbudget-chunking MUST NOT objectidentiteit definiëren; fusion van condition in recommendation is het verboden defaultpatroon. Machineclassificatie is een voorstel, nooit waarheid; unclassified blijft default tot menselijke bevestiging op die exacte objectversie; onbevestigde voorstellen MUST NOT gepubliceerd type zijn en MUST NOT `supported` zijn. Per-type classificatieregels (heading, definition, explanation, condition, exception, recommendation, unclassified) bepalen wat het type betekent, wat erin hoort, wat eruit hoort, en welke ambiguïteiten ALTIJD menselijk oordeel vereisen. Historische types (decision, action, score_rule, table, background, patient_information) MUST NOT MVP-servingtypes zijn. De gesloten relatieset is `applies_if`, `except_if`, `defines`, `explains`, `supported_by`, `supersedes`, `parent`/`child`; Operators MUST NOT relatietypes verzinnen; onbevestigde relaties MUST NOT binden. Een gepubliceerde recommendation MUST samen met gepubliceerde `applies_if`/`except_if`-targets worden geserveerd wanneer die in de gepubliceerde projectie bestaan. `exception` is ALTIJD high-risk (four-eyes). Four-eyes (tweede benoemde reviewer op het exacte objecttupel) MUST wanneer confirmed type `exception` is, `risk_level` high is, of een van de velden age_boundary, dosage, unit, score_points, score_threshold, operator, contraindication, exception, escalation_decision aanwezig is. Envelope `review_passes` MUST NOT publicatie autoriseren. Vanaf ieder kennisobject MUST de reviewer de exacte bronpassage kunnen openen; typebevestiging zonder die flow is niet aanvaardbaar; locators blijven Protocol v2.11. Deze protocolwijziging implementeert extract, relaties, open-origineel, schema of `publish()` niet. Azure/G2 blijven buiten deze delta. Protocol v2.14 wordt hier niet geschreven. Protocol v2.15 eist: het ingest-datumveld MUST een kalenderdatumkiezer zijn, geen vrijetekstvak; op scherm MUST de onderzoeker Europe/Amsterdam-kalendervolgorde `DD-MM-YYYY` zien en kiezen; intern MUST kernel/store/API/hash-envelope dezelfde dag als ISO `YYYY-MM-DD` bewaren (geen tijd, geen tijdzone); de datum MUST de datum op de freeze zijn (colofon / publicatiedatum); MUST NOT defaulten naar vandaag; MUST NOT het ingest-kliktijdstip zijn; leeg MUST NOT bij ingest; display-locale MUST NOT in opgeslagen bytes lekken; een EPD MUST ISO ontvangen. Het versieveld MUST NOT vrije tekst zijn; toegestaan patroon `^[0-9]+(\.[0-9]+)*$`; leeg MUST NOT bij ingest; MUST weigeren: `v`-prefix, letters, komma, voor-/naloopende punt, `-beta`/`-rc`, spaties, jaartal-als-versie. Deze twee velden zijn bronmetadata van de freeze, niet `object_version` en niet v2.14 `valid_from`/`valid_until`. Er MUST NOT een aparte onderzoekercontrole «zwaar/licht» of «snel/langzaam» zijn; bevestigd (of, alleen voor wachtrij-routing, voorgesteld) `object_type` bepaalt de reviewbaan; Extractie MUST `heading` voorstellen voor echte bronkoppen zodat die niet allemaal als `unclassified` landen; snelle baan is `heading` en MUST batch-bevestiging als structuur ondersteunen; trage baan is contenttypen plus `unclassified` tot bevestiging en blijft één object; de reviewlijst MUST een bronpassage-snippet tonen en MUST NOT de typenaam als rijtitel gebruiken; vierduizend unclassified-kaarten op één richtlijn is een fail van dit reviewoppervlak; MUST NOT bestaande gehashte Continentie-objecten stilzwijgend hersplitsen; Extractie MUST NOT gewone tekst automatisch tot `recommendation` promoveren; headings MUST NOT als handelingsadvies worden geserveerd; four-eyes blijft ongewijzigd; deze delta maakt G2 niet PASS en implementeert `publish()` niet. Deze protocolwijziging implementeert ingest-UI, versievalidatie, reviewwachtrijen, kernel of console-Python niet. Azure/G2 blijven buiten deze delta. Protocol v2.14 wordt hier niet geschreven en is niet de volgende stap. Protocol v2.16 eist: de Review-pagina MUST onderzoekers overtuigen; als zij die opent en die niet volstaat, is het project mislukt — ook als de kernel fail-closed is. Twee frustraties MUST NOT blijven (onduidelijk: twee deuren, rijen over de viewport, DOEN/OVERWEEG zonder uitleg; te kleine objecten: één woord, één getal, halve zin). Binnen één scherm MUST de onderzoeker weten welk document dit is, wat nu te doen is, en waarom (dit wordt wat een EPD MAG zeggen). UI-copy MUST onderzoekerstaal zijn; primaire actie MUST visueel overduidelijk zijn; kernel-ids MUST NOT de rijtitel zijn. MUST NOT twee deuren Openen plus Reviewen behouden die beide naar objectlijsten leiden; één documentkaart, één primaire knop Beoordeel. Twee benoemde stacks met counts: Koppen (echte TOC/sectietitels; batch-bevestigen als structuur, nooit als advies; headings MUST NOT als handelingsadvies) en Inhoud (definition, explanation, condition, exception, recommendation en unclassified tot getypt; één-objectkaart; high-risk four-eyes ongewijzigd). MUST NOT een pagina van duizenden identieke unclassified-titels. MUST NOT onderzoekercontrole «zwaar/licht». Compacte rijen: één regel freeze-bronzin (of echte koptekst) plus korte status; MUST NOT status/checkbox/tekst over de viewport spreiden; MUST NOT typenaam of kernel-document-id als titel. DOEN/OVERWEEG/NIET DOEN zijn stempels, geen objecten; MUST NOT GRADE-jargon; MUST NOT nieuw objecttype; MUST NOT rijen in Koppen; MUST NOT standalone kennisobjecten; gesloten waarden op type `recommendation` alleen: `doen` | `overweeg` | `niet_doen`; Extractie MUST NOT heading voorstellen voor die woorden; dit is GEEN fusion van condition in recommendation. Extractie MUST NOT tiny objects emitteren (alleen lijstnummer, alleen stempel, zinsfragment). Continentie is unpublished: een nieuwe extract van dezelfde freeze-bytes is VERPLICHT; source-hash blijft; unpublished objectidentiteiten MAGEN worden vervangen; MUST NOT in de UI liegen door fragmenten te verbergen zonder nieuwe extract. Serving/G2 ongewijzigd; four-eyes ongewijzigd; deze delta maakt G2 niet PASS en implementeert `publish()` niet. Deze protocolwijziging implementeert console, extract of Azure niet. Azure/G2 blijven buiten deze delta. Protocol v2.14 wordt hier niet geschreven en is niet de volgende stap. Protocol v2.17 eist: UI-copy MUST onderzoekerstaal zijn, geen slogans, geen «wat een EPD MAG zeggen», geen claim van één abonnee-klasse; de gehele zin «Dit wordt wat een EPD MAG zeggen.» MAG weg (niet verzwakken). Leadcopy zegt wat te doen op dit scherm (Beoordeel Koppen als structuur, Inhoud als kennisobjecten) zonder marketing. Via-negativa MUST NOT op onderzoekerspagina's, ook niet als ingeklapte help «Over deze console». Onderwerp/familie MUST leeg zijn op een verse nieuwe ingest (geen vooringevulde `continentie`; klasse blijft buiten deze delta). Bronpassage MUST dezelfde leesbare zin tonen als het kennisobject, zonder HTML-tags, CSS-klassenamen of kennisplatform-markup, op ieder object / de hele freeze (geen één-kaart-uitzondering; opgenomen antwoord op «Is dat hoe het definitief eruit gaat zien?» is NEE); v2.11-freezebytes en locators blijven exact (MUST NOT herserialiseren). Extractie MUST NOT kennisplatform-chrome als kennisobjecten of Koppen emitteren, ook niet als één-woord Tools; Tools/Home/Richtlijnen/Meedenken MUST NOT objecten zijn; Koppen = echte richtlijn-TOC van het richtlijnlichaam, geen sitenav. Stempel-UI (**Sterkte van de aanbeveling**, DOEN/OVERWEEG/NIET DOEN-picker) MUST NOT verschijnen behalve op type `recommendation`; een nav-woord MUST NOT een aanbevelingssterkte-controle krijgen. De relatielabel MUST direct naast het vinkje zitten; MUST NOT checkbox en doeltitel naar tegenoverliggende randen van de viewport spreiden. Deze regels gelden voor de hele freeze / ieder object / iedere sectie; Inleiding, Doel, Doelgroep en Aanleiding zijn voorbeelden, geen gesloten lijst. 2641 unclassified op één richtlijn blijft een fail van het reviewoppervlak. Unpublished Continentie MAG opnieuw worden geëxtraheerd; source-hash blijft; unpublished objectidentiteiten MAGEN worden vervangen; MUST NOT fragmenten verbergen zonder nieuwe extract. Serving/G2 ongewijzigd; four-eyes ongewijzigd; deze delta maakt G2 niet PASS en implementeert `publish()` niet. Deze protocolwijziging implementeert console, extract of Azure niet. Azure/G2 blijven buiten deze delta. Protocol v2.14 wordt hier niet geschreven en is niet de volgende stap. Protocol v2.18 eist: de open reviewkaart MUST de freeze-zin één keer tonen; MUST NOT dupliceren als zowel h3/titel als body; compacte rij/kaart: één bronzin plus korte status; de v2.16-compacte-rij-bar geldt voor de open kaart, niet alleen de lijst. Extractie MUST NOT een grammaticale voortzetting van de vorige zin tot een nieuw object splitsen; naloopzinnen (bijv. «Eventueel met hulp van de mantelzorger.», «Bijvoorbeeld …») MUST in hetzelfde kennisobject blijven als de zin die zij afmaken; dit herhaalt en verscherpt het v2.16 truncated-sentence / tiny-object-verbod (de v2.17 Continentie-re-extract faalde het nog). Extractie MUST NOT een tweede kennisobject emitteren waarvan de zichtbare freeze-proza (clean_text) identiek is aan een object dat al uit deze freeze is geëmitteerd; herhaalde HTML (samenvatting versus module) is geen extra kennis; onderscheiden echte koppen in verschillende secties MAGEN blijven («1.1 Inleiding» vs «2. Inleiding» zijn geen identieke strings). MUST NOT opgeslagen fragmenten verbergen zonder nieuwe extract; unpublished Continentie MAG opnieuw worden geëxtraheerd; source SHA-256 blijft; unpublished identiteiten MAGEN worden vervangen. Chrome Tools/Home is weg (v2.17 gehouden). Slogan is weg (v2.17 gehouden). Deze fail is duplicatie + truncated-sentence-split. Serving/G2/four-eyes/v2.14/Azure ongewijzigd; deze delta maakt G2 niet PASS en implementeert `publish()` niet. Deze protocolwijziging implementeert console, extract of Azure niet. Azure/G2 blijven buiten deze delta. Protocol v2.14 wordt hier niet geschreven en is niet de volgende stap. Protocol v2.19 eist: onderzoekers MUST NOT verplicht worden 2008 Inhoud-kaarten één voor één te openen; dat is dezelfde fail als 4000 unclassified: vermoeidheid, geen assurance; live bewijs: Inhoud (2008) / Koppen 78 op Continentie na de v2.18-extract (`main` `4ebfdbb88cdb`, snapshot `snap-ac59cf24f946088e-e402c4d3`); eigenaar zei dat het er beter uitziet, vroeg daarna of onderzoekers die 2008 objecten met de hand moeten doen, daarna «zet dat in protocol.». Koppen MAGEN en MUSTEN batch-bevestigbaar blijven als structuur, nooit als advies (v2.15/v2.16 ongewijzigd). De onderzoeker-verplichte trage review op een freeze is voorgestelde `recommendation`, plus `condition` / `exception` / ieder high-risk object (four-eyes ongewijzigd); die begrenzen het advies; dat MUSTEN zij met de hand doen. Resterende `unclassified` MUST NOT als gelijke één-voor-één-plicht van duizenden kaarten worden gepresenteerd; unclassified wordt nooit geserveerd (`supported` / handelingsadvies), dus 2000 klikken daarop voegen geen assurance toe. Serving blijft fail-closed: alleen bevestigde `recommendation` MAG `supported` / handelingsadvies; de machine MUST NOT beslissen dat iets licht genoeg is om te serveren; MUST NOT een onderzoekercontrole «zwaar/licht» of «snel/langzaam» (v2.15 ongewijzigd). Machineclassificatie blijft een voorstel, nooit gepubliceerd type; MUST NOT types auto-bevestigen; MUST NOT gewone tekst automatisch tot recommendation promoveren. Definition/explanation MAGEN bestaan en MAGEN later niet-adviesvragen beantwoorden eens bevestigd; zij zijn NIET de MVP-onderzoeker-2000-kaarten-plicht voor handelingsadvies. Extract SHOULD nog grover worden (kennisplatform-HTML is te praatgraag) maar de bar van deze delta is reviewPLICHT en wachtrijpresentatie, geen nieuw objecttype. v2.16 tiny-objects, v2.17 chrome, v2.18 geen-dubbele-zin / naloopzin / identieke clean_text blijven van kracht. 2008 unclassified/Inhoud-kaarten op één richtlijn blijft een fail van dit reviewoppervlak (zelfde bar als v2.15/v2.16). Unpublished Continentie MAG opnieuw worden geëxtraheerd; source SHA-256 blijft; unpublished identiteiten MAGEN worden vervangen; MUST NOT opgeslagen fragmenten verbergen zonder nieuwe extract. Serving/G2/four-eyes/v2.14/Azure ongewijzigd; deze delta maakt G2 niet PASS en implementeert `publish()` niet. Deze protocolwijziging implementeert console, extract of Azure niet. Azure/G2 blijven buiten deze delta. Protocol v2.14 wordt hier niet geschreven en is niet de volgende stap. Protocol v2.20 eist: PROTOCOL.md is de wet van V&VN Data Services voor iedere richtlijn, niet Continentie-only. Continentie verschijnt in v2.16–v2.19 als live bewijs van fails (stempels als Koppen, 2008 Inhoud-kaarten), niet als productidentiteit; die historische bewijszinnen MUST blijven en MUST NOT worden gestript; de volgende freeze MUST NOT Continentie hoeven zijn. Unpublished captured snapshots MAGEN van de operations console worden verwijderd door een geautoriseerde console-operator (zelfde klasse als ingest: benoemd researcher/reviewer-account, geen geheim engineer-pad). Dit is eigenaarsgeautoriseerde opruiming van unpublished capture, geen publicatie, geen G2, geen verbergen van fragmenten van een freeze die in Review blijft. MUST een echte verwijdercontrole op de documentkaart / Review-chooser voor unpublished snapshots alleen; label in onderzoekerstaal (bijv. Verwijder unpublished document); MUST bevestigen vóór uitvoering (destructief). Na delete MUST de snapshot NOT verschijnen op Inleveren/Review/Documentenhierarchie-lijsten; opgeslagen objecten+envelope voor die snapshot_id zijn weg; freeze-bytes van die unpublished bron MAGEN mee worden verwijderd; MUST NOT andere snapshots of `/home/data` globaal raken; MUST een audit-ledgerrij toevoegen (wie, wanneer, snapshot_id, bron-SHA-256, titel). Capture blijft geen publicatie. MUST NOT een gepubliceerde projectie of iets dat is gepubliceerd verwijderen; `publish()` blijft G2-BLOCKED; er is geen gepubliceerde Continentie. MUST NOT dit gebruiken om geselecteerde objecten binnen een freeze die in review blijft te verbergen (v2.16 hide-fragments-without-extract blijft); alleen het hele unpublished snapshot. MUST NOT SSH/wipe van `/home/data` als productpad; de console-actie is het pad. Four-eyes is niet vereist om unpublished capture te verwijderen; de uploader MAG unpublished die zij captureden verwijderen; een tweede benoemde reviewer is niet vereist voor delete (delete is geen type-bevestiging). Serving/G2/four-eyes/v2.14/Azure ongewijzigd; deze delta maakt G2 niet PASS en implementeert `publish()` niet. Deze protocolwijziging implementeert console, extract of Azure niet. Azure/G2 blijven buiten deze delta. Protocol v2.14 wordt hier niet geschreven en is niet de volgende stap. Beoordeel-timeout/performance is een apart issue en hoort niet in deze delta. Protocol v2.21 eist: eigenaar pauzeerde Cloud Shell en lockte een vier-golvenprogramma voor gecontroleerd gebruik met echte richtlijnbronnen; prioriteit is bronintegriteit, heldere kennisobjecten, veilige omgevingsscheiding, herstelbaarheid; volgorde MUST A daarna B daarna C daarna D; Cloud Shell / production ZIP blijven uit tot golf A op een gecontroleerde SHA staat. Deze delta MAPT bestaande wet, herschrijft die niet. Protocol v2.16–v2.18 verbieden al tiny objects, stempel-als-kop/object, naloopzinnen, chrome-objecten en identieke clean_text. Protocol v2.19 is duty-queue. Protocol v2.20 unpublished-delete staat al op main `ba3c85cec8e100e289e25e6a33fbf9440676c26e` en is GEEN vijfde golf. PROTOCOL.md is wet voor iedere richtlijn, niet Continentie-only (v2.20); Continentie-bewijszinnen in v2.16–v2.19 MUST blijven. G2-readiness (PR #69) pinte al azure-identity/azure-storage-blob en een report-only preflight; G2 blijft BLOCKED; `publish()` blijft G2-BLOCKED; RBAC Storage Blob Data Contributor op aidataservice/canonical-sources voor vvn-metis-console is extern. PR #82 is OPEN en MUST NOT worden geactiveerd tot de vier fouten zijn hersteld ÉN een Azure test-app bestaat. Golf A (volgende implementatie only): context-bewuste splitter + toetsbare reject-functie; een inhoudelijk kennisobject MUST één complete, zelfstandig leesbare betekeniseenheid zijn; MUST bronpassage+locator naar de freeze hebben; MUST NOT alleen een nummer, label, kopwoord, nav, stempel of zinsfragment zijn; MUST NOT identieke clean_text uit dezelfde freeze dupliceren; echte bronkoppen MAGEN als heading bestaan, alleen structuur, nooit advies, geen recommendation-stempel, batch-bevestigbaar als structuur; DOEN/OVERWEEG/NIET DOEN zijn geen objecten en geen Koppen; zij zijn een eigenschap van een volledige recommendation samen met de advieszin; splitter MUST naloop-/afhankelijke zinnen aan de vorige betekenisvolle zin hechten; MUST een stempel aan de onmiddellijk volgende advieszin hechten; MUST chrome/nav/lijstnummers/losse labels/leeg/te-kort FILTEREN VÓÓR objectaanmaak; MUST dubbele clean_text in dezelfde snapshot voorkomen; MUST freeze-bytes en locators exact houden (alleen afgeleide extract); reject-functie MUST weigeren: geen zelfstandige betekenis; onder een gedocumenteerde minimum-betekenisdrempel; alleen stempel/nummer/nav; grammaticale voortzetting van de vorige; identiek aan een eerder object uit dezelfde freeze; uitzonderingen MUST expliciet en getest zijn; korte echte definities en officiële koppen MUST NOT worden weggelaten; MUST NOT «Inleiding» als chrome behandelen; Home/Tools/Richtlijnen/Meedenken zijn chrome; Inleiding als echte sectietitel MAG heading blijven; regressiefixtures MUST van echte Continentie-failpatronen komen (stempels+advieszin, «Eventueel met hulp van de mantelzorger.», lijstnummers, Home/Tools, dubbele samenvatting/module, korte geldige definities, echte koppen op verschillende niveaus, HTML-herhaalde modules, PDF versus HTML); acceptatie: geen van die failpatronen landen als standalone inhoudelijke objecten in de review-plichtwachtrij; geen infrastructuur in golf A. Golf B (na A): G2-status MUST NOT van een stale static JSON-veld afhangen; read-only preflight MUST Blob-container bereikbaar, MI bruikbaar, vereiste Blob-rol aanwezig of toegang daadwerkelijk bewezen, container matcht actieve omgeving, bron kan byte-identiek worden opgeslagen en teruggelezen; gecontroleerde SHA-256-smoke; machineleesbaar bewijs (timestamp, environment, container, SHA-256, outcome); G2 PASS alleen na geslaagde gecontroleerde test, anders BLOCKED; de publicatiegate MUST NOT openen omdat een app-setting aanwezig is; deze protocol claimt geen G2 PASS; RBAC-grant blijft extern; MUST NOT Azure starten in golven A of B. Golf C (na B): PR #82 afmaken, niet activeren tot een test-App Service bestaat; packaging-script via bash of executable; Azure ZIP MUST dependencies bouwen of het artefact MUST volledig deploybaar zijn; git-archive-only is niet genoeg; live Oryx-during-deploy veroorzaakte HTTP_504 op B1; storage per omgeving via veilige app-settings, geen secrets in Git; gescheiden deploy-identiteiten zodat test niet production kan en production niet test; production is handmatig (alleen een volledige SHA al op main); MUST NOT runtime-data uit Git deployen; MUST NOT `/home/data` overschrijven; merge naar main MAG alleen naar test; de vier #82-fouten MUST worden hersteld; MUST NOT een test-App Service in deze protocol-PR aanmaken of activeren. Golf D (na C): inventaris van `/home/data/metis-console` (accounts/rollen, documentsnapshots, reviewbesluiten en audit-ledger, canonieke objecten, afgeleide projecties); export/backup; gecontroleerde restore naar een schone omgeving; integriteitscheck na restore; test die bewijst dat een deployment bestaande runtime-data niet verwijdert; geen grote databasemigratie; MUST de migratiegrens documenteren (een managed database wordt vereist vóór meerdere App Service-instances of gelijktijdige multi-reviewer-writes). Serving/G2/four-eyes/v2.14/Azure ongewijzigd; deze delta maakt G2 niet PASS en implementeert `publish()` niet. Deze protocolwijziging implementeert console, extract of Azure niet. Azure/G2 blijven buiten deze delta. Protocol v2.14 wordt hier niet geschreven en is niet de volgende stap. Protocol v2.22 eist: begrensde supersessie van Protocol v2.21 §3-volgorde A daarna B daarna C daarna D, alleen voor volgende-implementatievolgorde; golven zelf ongewijzigd; historische v2.21-golfdefinities blijven wet; golf A staat al in code op `main` `512ffa5026d06ff804434ddf4d07a08a36c02305`; volgende implementatie MUST golf C daarna golf D; MUST NOT deploy-test/deploy-production activeren tot Azure test-App Service `vvn-metis-console-test` bestaat; merge naar `main` MUST NOT automatisch deployen naar een ontbrekende test-app; ZIP MUST volledig deploybaar zijn inclusief dependencies; git-archive-only is niet genoeg; `--clean true` wist wwwroot en MUST NOT runtime-data verwijderen; DAARNA één Cloud Shell / production ZIP van die gecontroleerde SHA (A+C+D); geen live-URL-ingest; ingest van een nieuwe freeze MUST NA die ZIP, niet ervoor; live draait nog pre-golf-A extract tot ZIP; golf B (G2-bewijs/smoke) NA die ZIP; G2 blijft BLOCKED; `publish()` blijft G2-BLOCKED; ZIP opent publicatie niet; claimt geen G2 PASS; MUST NOT Azure test-app starten in deze protocol-PR; MUST NOT fragmenten verbergen; MUST NOT SSH-wipe van `/home/data`; MUST NOT v2.16–v2.21-bestanden herschrijven behalve index-/conflictpointers; MUST NOT C/D in deze PR implementeren; v2.20 unpublished-delete blijft op main, GEEN vijfde golf. Golf C (na A, vóór ZIP): PR #82 afmaken, niet activeren tot `vvn-metis-console-test` bestaat; packaging via bash of executable; Azure ZIP MUST volledig deploybaar zijn inclusief dependencies; git-archive-only is niet genoeg; storage per omgeving via veilige app-settings, geen secrets in Git; gescheiden deploy-identiteiten. Golf D (met C, vóór ZIP): inventaris van `/home/data/metis-console`; export/backup; gecontroleerde restore; integriteitscheck; bewijs dat `--clean true` wwwroot wist en MUST NOT runtime-data verwijdert; geen grote databasemigratie. Serving/G2/four-eyes/v2.14/Azure ongewijzigd; deze delta maakt G2 niet PASS en implementeert `publish()` niet. Deze protocolwijziging implementeert console, extract of Azure niet. Azure/G2 blijven buiten deze delta. Protocol v2.14 wordt hier niet geschreven en is niet de volgende stap. Protocol v2.23 eist: eigenaar vroeg Auditor om een code-oppervlaktereview op `main` HEAD `a566af56c8c88e76cb4de7fa51642b408705da02`; Auditor-oordeel REQUEST SIMPLIFICATION; geen GD-03, geen publicatie; G2 blijft BLOCKED; deze delta MUST NOT het live-pad wijzigen; volgende live-stap blijft William Cloud Shell ZIP van A+C+D SHA `a566af56`, daarna ingest van een freeze, daarna golf B; vereenvoudiging is NA die ZIP, niet in plaats daarvan; PR #82 blijft gesloten/ongemerged; eerste DELETE-snede, zero-caller src/-modules only (extract_pdf.py, semantic_transform.py, validation_workflow.py, build_second_review_queue.py, pre_step5_gate.py, import_expert_validation.py, reconcile_legacy_review.py, evaluate_safe_retrieval.py, build_retrieval_document.py); MUST NOT `src/semantic_transform_v2.py` verwijderen; MUST NOT `src/prepublication_gate_v2.py` verwijderen; MUST NOT `src/validation_workflow_v2.py` verwijderen; MUST NOT `src/apply_second_review.py` verwijderen; MUST NOT `src/canonical_store.py` verwijderen; service_app.py EN product_api_v1.py blijven; twee producten, geen leftovers; live ingest gebruikt extract_pdf_v2, extract_html_v1, semantic_transform_generic_v1, prepublication_gate_v3; operations_console_v1.py versus operations_console_app.py split blijft; geen template-engine; golf A splitter + reject_candidate blijven; test_protocol_v2_* en test_v2* blijven beide; scripts/run_integrity_sprint.sh MAG in dezelfde of de volgende wijziging naar committed fixtures wijzen; MUST NOT v2/v21/generic mergen in die PR; CLI review-queue versus console review_stacks / slow_review_duty is dual live path; Console is de onderzoeker-plichtwachtrij; plan, MUST NOT stilzwijgend verwijderen; volgende implementatie NA die ZIP is één deletion-PR; bestaande pytest only; MUST NOT splitter of console in die PR raken; Implementation MUST opnieuw bewijzen dat er geen live callers zijn vóór iedere delete; HANDOFF.md MUST NOT opnieuw worden aangemaakt. Serving/G2/four-eyes/v2.14/Azure ongewijzigd; deze delta maakt G2 niet PASS en implementeert `publish()` niet. Deze protocolwijziging implementeert console, extract of Azure niet. Azure/G2 blijven buiten deze delta. Protocol v2.14 wordt hier niet geschreven en is niet de volgende stap. De rollenset blijft GESLOTEN: researcher, reviewer, publisher; operators MUST NOT nieuwe roltypes verzinnen; geen open registratie; geen gedeelde login. AI, Grok Bot, Metis, Implementation engineer en Auditor MUST NOT als vereiste reviewers aanmaakbaar zijn. Uploader MUST NOT de enige vereiste reviewer zijn. Klasse promoveren MUST een nieuwe review vereisen; familiemove MUST NOT klinische herreview vereisen. Primaire actie per kamer MUST visueel overduidelijk zijn. Copy MUST zeggen wat de onderzoeker hier kan doen, wat daarna gebeurt en wat van hen wordt verwacht; via-negativa mag niet de primaire on-screen copy zijn. UI-vocabulaire MUST onderzoekerstaal zijn: document, titel, versie, familie, klasse, status, inleveren, review, publiceren; envelope is geen UI-term; geen snapshot-id als onderzoeker-invoerveld. Login MUST gebruikersnaam én wachtwoord vragen (`type=password`); geen gedeelde login; geen open registratie. Verplaatsen en promoveren MUST echte klikbare acties zijn op een zichtbaar document. Huisstyle: Rood `#E23100`, Paars `#5D3297`, Zwart `#000000`, Wit `#FFFFFF`; rood en zwart alleen accent; één secundaire familie per view; HK Grotesk (body), Raleway Bold (kopteksten, primaire knoppen, statements). Primaire DS-gebruikers zijn richtlijnonderzoekers (console) en B2B-abonnees (EPD, instelling, hun bot). Verpleegkundigen zijn geen primaire DS-gebruikers; ontwerp de console niet voor verpleegkundigen. Het MVP ondersteunt bronverwijzing en kennisrespons met provenance en abstention. De Product API is object-level retrieve-and-abstain; DS genereert geen proza; er is geen LLM in het MVP. RAG op kennisplatform-HTML is niet het product; DS is de eigen live gecureerde schakel, geen scrape. Een zorgapp-frontend, chatbot, EPD/ECD-UI, publieke website, beslisregel, patiëntspecifiek advies of getraind model is geen impliciet onderdeel van deze repository. Chat hoort niet in de console; eventuele chat is een latere consument van de Product API (G7/C6, U1/U2).

## Ontwikkelhiërarchie

De verplichte volgorde is:

`PROTOCOL.md → ROADMAP.md → acceptatietests → code`

De uitvoeringscyclus is:

`probleem of failure → protocoltoets → roadmapbesluit → tests → code → validatie → main`

De volledige werkwijze en uitzonderingsregels staan in [docs/DEVELOPMENT_WORKFLOW.md](docs/DEVELOPMENT_WORKFLOW.md).

## Wijzigingsregel

- Een bug die aantoonbaar strijdig is met een bestaande protocolregel wordt gerepareerd met tests; een nieuwe protocolversie is niet nodig.
- Een nieuwe of gewijzigde productregel, architectuurgrens, safety-invariant, verantwoordelijkheid of toegestane/verboden route vereist eerst een protocolwijziging.
- Als de norm tijdens implementatie onduidelijk blijkt, stopt de implementatie totdat het protocol of een formeel besluit de onduidelijkheid oplost.
