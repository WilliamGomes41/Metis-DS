# Baseline v0.1 test fixtures

These immutable fixtures support deterministic repository tests. They are copied
from the verified GitHub-ready v0.1.0 baseline and are not runtime output.

- `baseline_fixture_records.jsonl`: synthetic, published retrieval fixture.
- `fractuurpreventie_page15_raw.jsonl`: safe raw-fragment test fixture.
- `fractuurpreventie_page15_semantic.jsonl`: legacy semantic compatibility fixture.
- `fractuurpreventie_page15_semantic_v2.jsonl`: protocol v2 fixture.
- `fractuurpreventie_page15_semantic_v21.jsonl`: protocol v2.1 fixture.

These files are development fixtures. They are not evidence of canonical source
integrity, publication eligibility, or independent acceptance.
