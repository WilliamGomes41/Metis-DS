# V&VN Data Services — Actuele handoff

**Bijgewerkt:** 2026-08-28  
**Geldend protocol:** v2.8.0  
**Authoritative remote:** `WilliamGomes41/VENVN-DS` (public during the declared MVP period)  
**Default branch:** `main`

## Actuele waarheid

- Deze repository is **alleen** V&VN Data Services: protocol, bronintegriteit, pipeline, publicatiegates, retrieval, Product API en een interne operations console. Andere producten horen hier niet in governance of code.
- Protocol v2.8.0 is de normatieve baseline (v2.2.0 + v2.3-delta + v2.4-delta + v2.5-delta + v2.6-delta + v2.7-delta + v2.8-delta) en is live. Het checksumgebonden approval-manifest staat in `data/assurance/protocol_v2_8_approval.json`. Alle v2.6-consoleregels en alle v2.7-bron-/API-/distributieregels blijven van kracht, behalve de in v2.8 begrensde supersessie van de bouwvolgorde. De v2.6/v2.8-protocoldelta's zelf waren protocol-only: deze delta claimt geen bestaande UI en implementeert de console niet. Echte console-MVP ingest+review (family-tree, reviewerselectie, review return-loop; geen mockup) bestaat nu in code, gekoppeld aan de bestaande kernel (`src/operations_console_v1.py`, `src/operations_console_app.py`, `vvn-data-service serve-console`). Lokale G0-identiteit en `sources/private/` zijn de stand-in; publicatie blijft BLOCKED zonder immutable locator (G2).
- De gezaghebbende remote `WilliamGomes41/VENVN-DS` is public (`isPrivate: false`) onder de v2.5-MVP-uitzondering. Publiek is niet de latere productiestandaard; na de MVP MUST een nieuw plan private hosting of een organisatieplan herstellen.
- V&VN DS is vastgelegd als gevalideerde kennisobjecten, Product API (object-level retrieve-and-abstain; geen prozageneratie; geen LLM in het MVP), interne read-only inspection, en een interne operations console (v2.6-scope; console-MVP ingest+review nu in code) als menselijk oppervlak over de knowledge kernel. Primaire DS-gebruikers zijn richtlijnonderzoekers (console) en B2B-abonnees (EPD, instelling, hun bot). Verpleegkundigen zijn geen primaire DS-gebruikers; ontwerp de console niet voor verpleegkundigen. Een zorgapp-frontend, chatbot, EPD/ECD-UI en publieke website blijven verboden. Chat hoort niet in de console.
- RAG op kennisplatform-HTML is niet het product. DS is de eigen live gecureerde schakel, geen scrape. Train-ready gestructureerde objecten plus live publicatiestatuscheck blijven het B2B-aanbod.
- Het MVP is beperkt tot U1 bronverwijzing en U2 kennisrespons; U3 beslisregels, U4 patiëntspecifiek advies en U5 voorspellende/getrainde modellen zijn buiten scope. Training MAG alleen als tweede licentie als de client DS op vraagmoment nog aanroept om gepubliceerde status te controleren.
- Extern gebruik vereist een consumentenregistratie, gebruiksovereenkomst, expliciete verantwoordelijkheden en geteste update/withdrawal-verwerking. Het standaardproduct is een live retrieve-and-abstain-abonnement. De eerste betalende abonnee is een Nederlands EPD/ECD. Ziekenhuis- of universiteits-LLM-bots MAGEN op dezelfde wijze abonneren; DS bouwt die bots niet.
- PR #4 (source integrity), #5 (PDF completeness), #6 (scoped HTML), #7 (stack/G0), #8 (ontwikkelhiërarchie), #9 (v2.3-approval), #10 (integrity-kernel + runtime-entrypoints), #11 (repositoryscope), #12 (v2.4 product/distributie), #13 (v2.4-approval-evidence), #14 (GD-03 ESTABLISHED), #16 (v2.5 MVP public-remote exception), #18 (v2.6 internal operations console) en #19 (v2.7 source/API/distribution) zijn afgerond. PR #16 is squash-merged als `73c0669c8ad7c62a836e7a39666f3db33644be68` op 2026-08-27T22:10:04Z. PR #18 is squash-merged als `337728dd76c0b5d50bdc6421b83d6a777a134a61` op 2026-08-27T22:45:57Z. PR #19 is squash-merged als `d61aed6553503772df3fab16c4f12247966fc161` op 2026-08-28T00:30:00Z.
- Operationeel governance-record: `docs/GOVERNANCE.md` (ondergeschikt aan `PROTOCOL.md`, geen vijfde stuurlaag) met machineleesbaar GD-03-artefact `data/assurance/gd_03_c3_c6_reviewer_matrix.json`.
- GD-03 is ESTABLISHED (2026-08-27) as written: C3 min. 2 clinical+technical; C4 min. 2 evaluation+technical; C5 min. 2 security/operations+technical; C6 min. 3 clinical+technical+safety/evaluation. Reviewers onafhankelijk van de auteur; dezelfde exacte commit/snapshot. AI / Grok Bot / Metis / Implementation engineer / Auditor tellen niet mee als vereiste reviewer, keuren niet goed en publiceren niet. Het GD-03-governance-record blijft ESTABLISHED; dat record is geen nieuwe protocolversie. Benoemde GD-03-reviewers zijn niet bezet. GD-03 wordt niet heropend.
- Protocol v2.8 is een C5-wijziging (identiteit/toegang / publicatie / entitlement) die C3 (bron/review/publish-loop) omvat. Benoemde C5-reviewers zijn nog niet bezet. De projecteigenaar keurde de delta goed. Retrospectieve onafhankelijke technical- en security/operations-review van PR #4, PR #5, PR #16, Protocol v2.6 / PR #18, Protocol v2.7 / PR #19 en deze v2.8-delta blijft verschuldigd. Er worden geen reviewers verzonnen.
- Repository-indeling (C0, 2026-08-27): historische STEP-/audit-/repair-rapporten staan in `docs/history/`. De root is het operationele oppervlak (`PROTOCOL.md`, `ROADMAP.md`, `HANDOFF.md`, `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `SECURITY.md` plus code/config/build). Dit voegt geen vijfde stuurlaag toe. `output/` blijft als overgangsartefacten staan; tests gebruiken `data/fixtures/`.
- Fail-closed blijft ongewijzigd: geen bronbinaries in Git, geen secrets/keys/certs, `config/tenants.v1.json` blijft leeg, vertrouwelijke reviewartefacten blijven buiten Git, runtime-databases blijven buiten Git. `.gitignore` dekt dit en blijft staan. Fixtures, holdouts en getrackte `output/`-historie MAGEN in de publieke MVP-repo blijven; canonieke bron-HTML/PDF MAGEN dat niet. DS slaat geen ziekenhuisprotocollen, adoptielijsten of patiëntgegevens op. Geen Vercel, Neon of LLM-vendor.
- De exacte officiële HTML-representatie van bron 2 is lokaal geverifieerd en deterministisch geëxtraheerd.
- Bron 1 en bron 2 blijven `BLOCKED` voor publicatie zolang immutable opslag, definitieve registratie en klinische review ontbreken. Bron 2 is nog BLOCKED op duurzame immutable opslag. Publicatie blijft BLOCKED zonder immutable locator (G2).
- G0 Local Development is `PASS`. G0 Azure DEV blijft `BLOCKED`. G8 en Azure worden niet als `PASS` of gestart geclaimd. Console-identiteit sluit G8 niet en provisioneert geen Azure AD.
- G1 technische protection op `main` is ON. GitHub-ruleset **G1 main** (id `21686159`, aangemaakt 2026-08-27T22:10:53Z) is actief: `main` mag niet worden verwijderd; geen force-push / non-fast-forward; required status checks `test (3.12)` en `test (3.13)` (strict); pull request verplicht vóór merge; 0 vereiste goedkeurende reviews (solo owner). Protected branch, required CI en PR-workflow bestaan nu. Named GD-03-reviewers zijn niet bezet.

## Open governancepunt

GD-01, GD-02, GD-04, GD-05, GD-06 en GD-07 blijven OPEN. GD-03 is niet langer OPEN.

Benoemde individuele C3–C6-reviewers zijn een latere bezettingsstap en houden GD-03 niet OPEN. Tot die bezetting kunnen C3–C6-merges in de praktijk niet voldoen aan de vastgestelde matrix, behalve reparaties die aantoonbaar een bestaande protocolregel herstellen en eigenaarsgoedgekeurde C5-protocoldelta's (v2.5 / PR #16, v2.6 / PR #18, v2.7 / PR #19 en v2.8) met retrospectieve review.

PR #4, PR #5, PR #16, Protocol v2.6 / PR #18, Protocol v2.7 / PR #19 en Protocol v2.8 zijn C5-wijzigingen en vereisen nog retrospectieve onafhankelijke technical- en security/operations-review volgens GD-03.

Evidence: `docs/GOVERNANCE.md` en `data/assurance/gd_03_c3_c6_reviewer_matrix.json`.

## Protocolwijziging 2026-08-28 (v2.6, live als scope)

Protocol v2.6 keurt een interne operations console goed als menselijk oppervlak over de knowledge kernel. Dat is een begrensde supersessie van de v2.4-lezing «geen productfrontend in deze repository». Zorgapp-frontend, chatbot, EPD/ECD-UI en publieke website blijven verboden. Chat is geen kamer in de console. Vier kamers: Ingest, Review (verplichte return-loop; uploader MUST NOT de enige vereiste reviewer zijn), Publish, Analytics (laatst). Identiteit (researcher/reviewer/publisher) is een vereiste consolecapability; provider blijft onder G0. De console bestaat niet in code. G1 blijft ON. De remote blijft public onder v2.5. Bron 2 blijft BLOCKED op immutable opslag.

## Protocolwijziging 2026-08-28 (v2.7 live)

Protocol v2.7 legt de eigenaarsgoedgekeurde design grilling vast. First-wave officiële bestanden zijn HTML-pagina en PDF; kennisplatform `story.html`-boomplayers vallen buiten de first wave; de officiële file is de kennisplatform-freeze, geen levend Word-document. Ingest is bestandsupload of URL; een URL MUST onmiddellijk naar exacte bytes worden gesnapshot. Console-bronboom: één trunk-richtlijn plus gehashte branch-documenten; researcher-UX, geen verpleegkundig beslissingsboom. Nieuwe richtlijnversie = nieuw snapshot, object-diff; oude release blijft live tot cutover-publish. API: alleen object-level retrieve; ongepubliceerde branch-objecten abstainen ook als de trunk gepubliceerd is; `supported` draagt V- en VN-labels; DS genereert geen proza; abstain is een gesloten zinnencatalogus in de console; geen LLM in het MVP. Tenant = wie de API MAG aanroepen; alle gepubliceerde V- en VN-objecten serveren; geen ziekenhuisprotocollen, adoptielijsten of patiëntgegevens opslaan. Distributie: asset is live curatie; standaardproduct is live retrieve-and-abstain-abonnement; training alleen als tweede licentie mét live publicatiestatuscheck; eerste betalende abonnee is een Nederlands EPD/ECD; ziekenhuis-/uni-LLM-bots MAGEN op dezelfde wijze abonneren; DS bouwt die bots niet. Analytics later: welke objecten zijn gevraagd; care-impact-onderzoek hoort niet bij DS; geen federated learning. Alle v2.6-consoleregels blijven van kracht. Console blijft goedgekeurd-niet-gebouwd. G1 blijft ON. G0 Azure DEV blijft BLOCKED. Geen Vercel, Neon of LLM-vendor.

## Protocolwijziging 2026-08-28 (v2.8 live)

Protocol v2.8 legt de eigenaarsgoedgekeurde evaluatie van de grilling vast. Primaire DS-gebruikers zijn richtlijnonderzoekers (console) en B2B-abonnees (EPD, instelling, hun bot); verpleegkundigen zijn geen primaire gebruikers; ontwerp de console niet voor verpleegkundigen. Bronhiërarchie heeft twee assen: klasse/gewicht op ieder object (`richtlijn` > `handreiking` > `artikel` > `transcript`/`podcast`; zwaarder MAG niet door lichter worden gevuld; een podcast MUST NOT een richtlijn in de API vervangen, ook niet in dezelfde familie; lagere klasse MAG `supported` alleen mét klaslabel en MUST NOT een gat van een ontbrekende hogere klasse vullen als die hogere klasse in het gepubliceerde corpus bestaat) en familie/topic (bijvoorbeeld continentie) als haak, geen nieuw bestand. MVP: ingest-onderzoeker zet de familie; een branch morgen toevoegen tekent de boom niet opnieuw; verplaatsen tussen families vereist geen klinische herreview (curatoract); klasse promoveren (transcript naar richtlijn) vereist wel review; als later blijkt dat familiemoves betekenis veranderen is dat een nieuwe protocolwijziging, geen stille extra review. Console-boom = familie × klasse; ieder bestand houdt zijn eigen hash; API blijft object-level: alleen wat gevraagd is; ongepubliceerde branches abstainen. RAG op kennisplatform-HTML is niet het product; DS is de eigen live gecureerde schakel, geen scrape; train-ready gestructureerde objecten plus live publicatiestatuscheck blijven het B2B-aanbod. Bouwvolgorde: geen mockup; niet wachten op Azure of een af DS; volgende implementatie is een echte console-MVP (ingest HTML/PDF, family-tree, reviewers selecteren, review return-loop) op de bestaande kernel; Continentie bron 2 komt VIA die console binnen, niet via een parallel engineer-only pad als onderzoekerservaring; Product API bestaat al, niet eerst herbouwen; Azure DEV blijft BLOCKED onder G0; analytics later; publish-kamer klein nadat de review-loop werkt. Dit slaat duurzame immutable opslag niet over: lokale store is de console-stand-in tot G0 Azure DEV; publicatie blijft BLOCKED zonder immutable locator (G2). Console blijft goedgekeurd-niet-gebouwd in deze protocol-PR. G1 blijft ON. Remote blijft public onder v2.5. Geen Vercel, Neon of LLM-vendor. Benoemde GD-03-reviewers blijven onbezet. C5 spanning C3, eigenaarsgoedgekeurd, GD-03 ESTABLISHED, retrospectieve C5 blijft verschuldigd. Er worden geen namen verzonnen.

## Eerstvolgende taak

1. Duurzame immutable locator (G2) voor bron 2: de console-MVP kan Continentie nu als envelope vastleggen onder lokale `sources/private/` (G0-stand-in, expliciet geen productie), maar publicatie en cutover blijven BLOCKED tot er een echte immutable locator is. Geen Azure starten. Geen mock-data als gepubliceerd serveren.
2. Retrospectieve onafhankelijke C5-review van PR #4, PR #5, PR #16, Protocol v2.6 / PR #18, Protocol v2.7 / PR #19, Protocol v2.8 en deze console-implementatie volgens de GD-03-matrix; daarna named reviewers bezetten.
3. Holdout B onafhankelijk vergrendelen; geen tuning daarop. GD-01 en GD-02 blijven OPEN tot die gates.
4. Na de MVP: een nieuw plan dat private hosting of een organisatieplan herstelt.
5. Pas na de MVP-gates een U1/U2-consumentencontract en partnerpilot ontwerpen. Eerste betalende abonnee is een Nederlands EPD/ECD; DS bouwt geen EPD/ECD of bots.

## Hervattingsinstructie

Bij hervatting worden achtereenvolgens volledig gelezen:

1. `PROTOCOL.md` en de gekoppelde geldende protocolspecificaties, plus het ondergeschikte record `docs/GOVERNANCE.md`;
2. `ROADMAP.md`;
3. `HANDOFF.md`;
4. de relevante acceptatie- en invarianttests;
5. pas daarna de implementatiecode.

Na iedere materiële merge wordt deze handoff in dezelfde PR bijgewerkt met: mergecommit, afgeronde gate, resterende blokkades, afwijkingen en één eerstvolgende concrete taak.
