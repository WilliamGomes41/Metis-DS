# V&VN Data Services — Actuele handoff

**Bijgewerkt:** 2026-08-31  
**Geldend protocol:** v2.13.0 + v2.12.0 + v2.11.0  
**Authoritative remote:** `WilliamGomes41/VENVN-DS` (public during the declared MVP period)  
**Default branch:** `main`

## Actuele waarheid

- Deze repository is **alleen** V&VN Data Services: protocol, bronintegriteit, pipeline, publicatiegates, retrieval, Product API en een interne operations console. Andere producten horen hier niet in governance of code.
- Protocol v2.13.0 plus Protocol v2.12.0 plus Protocol v2.11.0 is de normatieve baseline (v2.2.0 + v2.3-delta + v2.4-delta + v2.5-delta + v2.6-delta + v2.7-delta + v2.8-delta + v2.9-delta + v2.10-delta + v2.11-delta + v2.12-delta + v2.13-delta) en is live. Het checksumgebonden approval-manifest voor v2.13 staat in `data/assurance/protocol_v2_13_approval.json` (`commit_sha` is `8cfe9dc37a8f3f0bf3548a40abff41acb4fb39ce`); voor v2.12 in `data/assurance/protocol_v2_12_approval.json`; voor v2.11 in `data/assurance/protocol_v2_11_approval.json`. De vier lagen blijven bron/evidence → canonieke kennis → governance → product. Eén kennisobject MUST één bevestigbare betekeniseenheid zijn; context leeft in gereviewde relaties, niet in een blob; de canonieke store is de enige bron van waarheid. Alle v2.6-consoleregels, alle v2.7-bron-/API-/distributieregels, alle v2.8-gebruikers-/hiërarchieregels, alle v2.9-UX-/huisstyleregels, alle v2.10-console-/nav-/accountsregels, alle v2.11-freeze-/locatorregels en alle v2.12-type-/review-/projectieregels blijven van kracht, behalve de in v2.11 begrensde supersessie van de v2.7-zin dat ingest MUST een URL accepteren zonder HTML van PDF te onderscheiden, de in v2.12 begrensde supersessie van extractie-betekenis, «alleen recommendations zijn supported», envelope-`review_passes` als publicatieautorisatie, live-governance-per-query, en de in v2.13 begrensde supersessie van tokenbudget-identiteit, fusion, machineclassificatie-als-waarheid, schema-v1.2-relatienamen-als-serving-wet, onbevestigde relaties, high-risk zonder four-eyes, `extraction_rules_v0.1` als serving-taxonomie, en de eerstvolgende implementatie. De v2.6/v2.8-protocoldelta's zelf waren protocol-only: deze delta claimt geen bestaande UI en implementeert de console niet. Echte console-MVP ingest+review (family-tree, reviewerselectie, review return-loop; geen mockup) bestaat nu in code, gekoppeld aan de bestaande kernel (`src/operations_console_v1.py`, `src/operations_console_app.py`, `vvn-data-service serve-console`). Protocol v2.9 legt taakgerichte onderzoeker-UX en de V&VN digitale stylesheet vast; deze v2.9-delta implementeert die rewrite niet. De console-UX-rewrite is nu in code (PR #25): taakgerichte kamers, onderzoekerstaal (envelope is geen UI-term; geen snapshot-id als onderzoeker-invoerveld), zichtbare verplaats-/promoveeracties, login met gebruikersnaam én wachtwoord (`type=password`), V&VN digitale stylesheet (Water-secundaire familie, systeemfont-fallback) en het officiële v&vn-beeldmerk (`assets/brand/venvn-beeldmerk.png`). Kernelregels in `src/operations_console_v1.py` blijven intact. Protocol v2.10 legt Documentenhierarchie, wachttaak-badges en de Accounts-kamer vast; deze v2.10-delta implementeert die follow-up niet. Het v2.10-console-vervolg is in code (inclusief PR #31 UI-spelling Documentenhiërarchie) en wordt niet heropend. Protocol v2.11 legt geüploade HTML-freeze, weigering van live URL-HTML, verplichte source locators en fail-closed Product API zonder locator vast; deze v2.11-delta implementeert die ingest-weigering en API-fail-closed niet. HTML wordt niet geheel verboden. Capture is geen publicatie. Protocol v2.12 legt objecttype (unclassified-default), reviewbinding aan het objecttupel en atomaire gepubliceerde projectie vast; deze v2.12-delta implementeert extract/API/console niet. Protocol v2.13 legt atomaire objecten, per-type classificatieregels, gesloten relaties en high-risk four-eyes vast; deze v2.13-delta implementeert extract, relaties, open-origineel, schema of `publish()` niet. PROTOCOL → tests → code later. Metis implementeert niet. Azure/G2 blijven buiten deze delta: geen locator-implementatie, geen Blob, geen G2-PASS. Lokale G0-identiteit en `sources/private/` zijn de stand-in; publicatie blijft BLOCKED zonder immutable locator (G2). De G2-locator blijft de publicatieblocker. De eigenaarsgoedgekeurde implementatiegolf A+B+C (niet die protocol-PR's) legt v2.12-kernel, v2.11-ingestlock en het v2.10-console-vervolg nu in code vast. Eigenaarslock 2026-08-29 (docs-only, via Metis; geen productcode): de reviewer-bronpassage MUST twee kolommen zijn op de reviewkaart — links het kennisobject (type, relatietickboxes, besluit), rechts de exacte bronpassage (v2.11-locators). Smalle schermen stapelen. Geen nieuw locatorschema. Typebevestiging blijft geblokkeerd als de passage niet open kan. Relaties blijven voorgestelde checkboxes + bevestigen; MUST NOT een graaf-editor, drag-and-drop of pijlen-tekenen-UI. Een read-only schets van al bevestigde links MAG later bij een te vol Continentie-document; niet de volgende code van deze lock. De volgende console-implementatie na deze docs-lock (niet deze PR) is alleen die gesplitste reviewkaart. DAARNA wacht G2 nog op Azure. `publish()` blijft G2-BLOCKED. Protocol v2.14 wordt in deze PR niet geschreven.
- De gezaghebbende remote `WilliamGomes41/VENVN-DS` is public (`isPrivate: false`) onder de v2.5-MVP-uitzondering. Publiek is niet de latere productiestandaard; na de MVP MUST een nieuw plan private hosting of een organisatieplan herstellen.
- V&VN DS is vastgelegd als gevalideerde kennisobjecten, Product API (object-level retrieve-and-abstain; geen prozageneratie; geen LLM in het MVP), interne read-only inspection, en een interne operations console (v2.6-scope; console-MVP ingest+review nu in code) als menselijk oppervlak over de knowledge kernel. Primaire DS-gebruikers zijn richtlijnonderzoekers (console) en B2B-abonnees (EPD, instelling, hun bot). Verpleegkundigen zijn geen primaire DS-gebruikers; ontwerp de console niet voor verpleegkundigen. Een zorgapp-frontend, chatbot, EPD/ECD-UI en publieke website blijven verboden. Chat hoort niet in de console.
- RAG op kennisplatform-HTML is niet het product. DS is de eigen live gecureerde schakel, geen scrape. Train-ready gestructureerde objecten plus live publicatiestatuscheck blijven het B2B-aanbod.
- Het MVP is beperkt tot U1 bronverwijzing en U2 kennisrespons; U3 beslisregels, U4 patiëntspecifiek advies en U5 voorspellende/getrainde modellen zijn buiten scope. Training MAG alleen als tweede licentie als de client DS op vraagmoment nog aanroept om gepubliceerde status te controleren.
- Extern gebruik vereist een consumentenregistratie, gebruiksovereenkomst, expliciete verantwoordelijkheden en geteste update/withdrawal-verwerking. Het standaardproduct is een live retrieve-and-abstain-abonnement. De eerste betalende abonnee is een Nederlands EPD/ECD. Ziekenhuis- of universiteits-LLM-bots MAGEN op dezelfde wijze abonneren; DS bouwt die bots niet.
- PR #4 (source integrity), #5 (PDF completeness), #6 (scoped HTML), #7 (stack/G0), #8 (ontwikkelhiërarchie), #9 (v2.3-approval), #10 (integrity-kernel + runtime-entrypoints), #11 (repositoryscope), #12 (v2.4 product/distributie), #13 (v2.4-approval-evidence), #14 (GD-03 ESTABLISHED), #16 (v2.5 MVP public-remote exception), #18 (v2.6 internal operations console), #19 (v2.7 source/API/distribution), #21 (v2.8 users/hierarchy/console-order), #23 (console-MVP ingest+review), #24 (Protocol v2.9), #25 (console-UX-rewrite), #26 (Protocol v2.10) en #29 (Protocol v2.12) zijn afgerond. PR #16 is squash-merged als `73c0669c8ad7c62a836e7a39666f3db33644be68` op 2026-08-27T22:10:04Z. PR #18 is squash-merged als `337728dd76c0b5d50bdc6421b83d6a777a134a61` op 2026-08-27T22:45:57Z. PR #19 is squash-merged als `d61aed6553503772df3fab16c4f12247966fc161` op 2026-08-28T00:30:00Z. PR #21 is squash-merged als `280f462752a2a7e4928cd96bbdb8157d3cdd8c28` op 2026-08-28. PR #23 is gemerged als `84ccb3dfc632802836ffee849c324d73a4bb8bb6` op 2026-08-28T10:26:02Z. PR #24 is gemerged als `48fc309032a693b9efcebbf4a7bbe9774cba51fa` op 2026-08-28. PR #25 is gemerged als `f8d904b64c84925b0faa1d09c2d5b32a40566720` op 2026-08-28. PR #26 is gemerged als `46c880c090944ffb5a2025c71d3b1a8414158f99` op 2026-08-28. PR #29 is gemerged als `6bc816eb2c3969601c8a1cb2befe264fa602bd54` op 2026-08-28.
- Operationeel governance-record: `docs/GOVERNANCE.md` (ondergeschikt aan `PROTOCOL.md`, geen vijfde stuurlaag) met machineleesbaar GD-03-artefact `data/assurance/gd_03_c3_c6_reviewer_matrix.json`.
- GD-03 is ESTABLISHED (2026-08-27) as written: C3 min. 2 clinical+technical; C4 min. 2 evaluation+technical; C5 min. 2 security/operations+technical; C6 min. 3 clinical+technical+safety/evaluation. Reviewers onafhankelijk van de auteur; dezelfde exacte commit/snapshot. AI / Grok Bot / Metis / Implementation engineer / Auditor tellen niet mee als vereiste reviewer, keuren niet goed en publiceren niet. Het GD-03-governance-record blijft ESTABLISHED; dat record is geen nieuwe protocolversie. Benoemde GD-03-reviewers zijn niet bezet. GD-03 wordt niet heropend.
- Protocol v2.8 is een C5-wijziging (identiteit/toegang / publicatie / entitlement) die C3 (bron/review/publish-loop) omvat. Protocol v2.9 is een C3-wijziging (de console als menselijke deur van de bron/review/publish-loop: taakgerichte onderzoeker-UX en V&VN digitale stylesheet). Protocol v2.10 is een C5-wijziging (identiteit/toegang) die C3 (console-kamers/nav) omvat: Documentenhierarchie, wachttaak-badges en Accounts-kamer. Protocol v2.11 is een C3-wijziging (bron/review/publish / retrieve-safety: geüploade HTML-freeze, weigering van live URL-HTML, verplichte source locators, fail-closed Product API zonder locator). Protocol v2.12 is een C3-wijziging (retrieve-safety / answerability) die C5 alleen als review/publish-autorisatiebinding omvat: objecttype, reviewtupel, atomaire projectie. Protocol v2.13 is een C3-wijziging (retrieve-safety / answerability / knowledge model) die C5 als high-risk four-eyes review/publish-autorisatie omvat: atomaire objecten, per-type classificatie, gesloten relaties, four-eyes. Benoemde C3- en C5-reviewers zijn nog niet bezet. De projecteigenaar keurde de delta goed. Retrospectieve onafhankelijke review van PR #4, PR #5, PR #16, Protocol v2.6 / PR #18, Protocol v2.7 / PR #19, Protocol v2.8 / PR #21, de console-implementatie / PR #23, Protocol v2.9 / PR #24, de console-UX-rewrite / PR #25, Protocol v2.10 / PR #26, Protocol v2.12 / PR #29, Protocol v2.11 en deze v2.13-delta blijft verschuldigd. Er worden geen reviewers verzonnen.
- Repository-indeling (C0, 2026-08-27): historische STEP-/audit-/repair-rapporten staan in `docs/history/`. De root is het operationele oppervlak (`PROTOCOL.md`, `ROADMAP.md`, `HANDOFF.md`, `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `SECURITY.md` plus code/config/build). Dit voegt geen vijfde stuurlaag toe. `output/` blijft als overgangsartefacten staan; tests gebruiken `data/fixtures/`.
- Fail-closed blijft ongewijzigd: geen bronbinaries in Git, geen secrets/keys/certs, `config/tenants.v1.json` blijft leeg, vertrouwelijke reviewartefacten blijven buiten Git, runtime-databases blijven buiten Git. `.gitignore` dekt dit en blijft staan. Fixtures, holdouts en getrackte `output/`-historie MAGEN in de publieke MVP-repo blijven; canonieke bron-HTML/PDF MAGEN dat niet. DS slaat geen ziekenhuisprotocollen, adoptielijsten of patiëntgegevens op. Geen Vercel, Neon of LLM-vendor.
- De exacte officiële HTML-representatie van bron 2 is lokaal geverifieerd en deterministisch geëxtraheerd.
- Bron 1 en bron 2 blijven `BLOCKED` voor publicatie zolang immutable opslag, definitieve registratie en klinische review ontbreken. Bron 2 is nog BLOCKED op duurzame immutable opslag. Publicatie blijft BLOCKED zonder immutable locator (G2).
- G0 Local Development is `PASS`. G0 Azure DEV blijft `BLOCKED`. G8 en Azure worden niet als `PASS` of gestart geclaimd. Console-identiteit sluit G8 niet en provisioneert geen Azure AD.
- G2-bronstore 2026-08-31 (eigenaarsopdracht): V&VN-tenant `venvn.nl`, abonnement Microsoft Azure `8c829c96-1784-4947-8a2b-92027c51fec9`, resourcegroep `AI_Dataservice`, opslagaccount `aidataservice` (westeurope, StorageV2, ZRS), privécontainer `canonical-sources`. Locator `azure://aidataservice/canonical-sources/{sha256}/{filename}`. Container leeg. G2 blijft `BLOCKED`. Geen keys in Git. Runtime/Postgres/Foundry niet gestart.
- G1 technische protection op `main` is ON. GitHub-ruleset **G1 main** (id `21686159`, aangemaakt 2026-08-27T22:10:53Z) is actief: `main` mag niet worden verwijderd; geen force-push / non-fast-forward; required status checks `test (3.12)` en `test (3.13)` (strict); pull request verplicht vóór merge; 0 vereiste goedkeurende reviews (solo owner). Protected branch, required CI en PR-workflow bestaan nu. Named GD-03-reviewers zijn niet bezet.

## Implementatievoortgang (deze golf)

Eigenaarsgoedgekeurde codegolf A→B→C plus v2.13-kernelfollow-up op de bestaande kernel, één PR tegen main. De protocol-delta's zelf implementeerden geen productcode; wat nu in code bestaat:

- A (v2.12): extractie default `unclassified` (heading MAG `heading`); machinevoorstel is geen gepubliceerd type; answerability is vraagtype × objecttype; alleen `recommendation` is handelingsadvies; reviewautorisatie is het objecttupel, niet envelope-`review_passes`; Product API leest een atomaire gepubliceerde projectie; `publish()` blijft G2-BLOCKED.
- B (v2.11-lock in code; Protocol v2.11.0 is al live op main): live URL-HTML geweigerd; geüploade freeze-HTML en PDF-upload OK; PDF-URL alleen met onmiddellijke exacte-byte-hash; `supported` vereist aanwezige `source_locator`. Deze implementatie-PR bewerkt PROTOCOL.md en PROTOCOL_V2_* niet.
- C (v2.10): UI-heading Documentenhierarchie; echte wachttaak-badges (zero-hidden); Accounts-kamer met gesloten rollen researcher/reviewer/publisher; verboden identiteiten niet als vereiste reviewer aanmaakbaar.
- D (v2.13): Implementation engineer op de bestaande kernel — atomaire split op betekenisgrenzen (tokenbudget definieert geen objectidentiteit; fusion van condition/exception in recommendation verboden behalve één grammaticale claim); gesloten serving-relaties (`applies_if`, `except_if`, `defines`, `explains`, `supported_by`, `supersedes`, `parent`/`child`); onbevestigde relaties binden niet; schema v1.2 aligned op serving-wetnamen; historische types niet geserveerd; four-eyes op het v2.12-tupel (`exception`, high `risk_level`, listed risk fields; agents tellen niet; uploader MUST NOT de enige vereiste reviewer zijn); open-origineel via freeze-bytes + v2.11-locator in de reviewkamer; Product API serveert een gepubliceerde recommendation samen met gepubliceerde `applies_if`/`except_if`-targets. `publish()` blijft G2-BLOCKED. Capture is geen publicatie.
- E (console review completeness, PR #35): reviewkaart post voorgestelde relaties naar `confirm_relations()`; type-select heeft disabled «nog niet bevestigd» en keurt niet stilzwijgend `heading` goed; type/approve vereisen open-origineel; `published_object_type()` serveert alleen `confirmed_object_type` in de gesloten set (geen `published_at` / ontbrekende-proposed-key-shortcuts); GET `/v1/knowledge/{id}` abstaint of 404 zonder locator of met unclassified/historisch type. Rollen wijzigen via bestaande `assign_roles()`. `publish()` blijft G2-BLOCKED.
- F (eigenaarslock 2026-08-29, docs-only): reviewkaart twee kolommen (links kennisobject, rechts exacte bronpassage; smal stapelt). Relaties blijven voorgestelde checkboxes + bevestigen. MUST NOT een graaf-editor. Niet geïmplementeerd in deze PR. `publish()` blijft G2-BLOCKED.

Resterende blokkades: gesplitste reviewkaart (nog niet in code), G2-locator, G0 Azure DEV, named GD-03-reviewers, retrospectieve C3/C5. Geen Azure/Vercel/Neon/LLM. `tenants.v1.json` blijft leeg. Eerstvolgende taak: tweecoloms reviewkaart (bronpassage). DAARNA G2. Protocol v2.14 is niet de volgende stap. Metis implementeert niet. Deze docs-PR implementeert de kaart niet.

## Eigenaarslock 2026-08-29 (docs-only)

Owner William, via Metis. Geen productcode. Geen Protocol v2.14. Geen Azure. Geen relatiegraaf.

Reviewer-bronpassage is twee kolommen op de reviewkaart: links het kennisobject (type, relatietickboxes, besluit); rechts de exacte bronpassage (v2.11-locators). Smalle schermen stapelen. Geen nieuw locatorschema. Typebevestiging blijft geblokkeerd als de passage niet open kan.

Relaties blijven voorgestelde checkboxes + bevestigen. MUST NOT een graaf-editor, drag-and-drop of pijlen-tekenen-UI bouwen. Een read-only schets van al bevestigde links MAG later wanneer een echt Continentie-document te veel objecten voor één kaart heeft; niet de volgende code van deze lock.

Volgende console-implementatie na deze docs-lock (niet deze PR): alleen de gesplitste reviewkaart. DAARNA wacht G2 nog op Azure. `publish()` blijft G2-BLOCKED. Metis implementeert niet.

## Open governancepunt

GD-01, GD-02, GD-04, GD-05, GD-06 en GD-07 blijven OPEN. GD-03 is niet langer OPEN.

Benoemde individuele C3–C6-reviewers zijn een latere bezettingsstap en houden GD-03 niet OPEN. Tot die bezetting kunnen C3–C6-merges in de praktijk niet voldoen aan de vastgestelde matrix, behalve reparaties die aantoonbaar een bestaande protocolregel herstellen, eigenaarsgoedgekeurde C5-protocoldelta's (v2.5 / PR #16, v2.6 / PR #18, v2.7 / PR #19, v2.8 / PR #21 en v2.10) en de eigenaarsgoedgekeurde C3-protocoldelta's v2.9, v2.11, v2.12 en v2.13, telkens met retrospectieve review.

PR #4, PR #5, PR #16, Protocol v2.6 / PR #18, Protocol v2.7 / PR #19, Protocol v2.8 / PR #21, de console-implementatie / PR #23 en Protocol v2.10 zijn C5-wijzigingen en vereisen nog retrospectieve onafhankelijke technical- en security/operations-review volgens GD-03. Protocol v2.9, Protocol v2.11 en Protocol v2.12 zijn C3 (eigenaarsgoedgekeurd) en vereisen nog retrospectieve clinical- en technical-review. Protocol v2.12 is C3 spanning C5 (eigenaarsgoedgekeurd) en vereist nog retrospectieve clinical- en technical-review. Protocol v2.13 is C3 spanning C5 four-eyes (eigenaarsgoedgekeurd) en vereist nog retrospectieve clinical- en technical-review; GD-03 wordt niet heropend. Er worden geen namen verzonnen.

Evidence: `docs/GOVERNANCE.md` en `data/assurance/gd_03_c3_c6_reviewer_matrix.json`.

## Protocolwijziging 2026-08-28 (v2.6, live als scope)

Protocol v2.6 keurt een interne operations console goed als menselijk oppervlak over de knowledge kernel. Dat is een begrensde supersessie van de v2.4-lezing «geen productfrontend in deze repository». Zorgapp-frontend, chatbot, EPD/ECD-UI en publieke website blijven verboden. Chat is geen kamer in de console. Vier kamers: Ingest, Review (verplichte return-loop; uploader MUST NOT de enige vereiste reviewer zijn), Publish, Analytics (laatst). Identiteit (researcher/reviewer/publisher) is een vereiste consolecapability; provider blijft onder G0. De console bestaat niet in code. G1 blijft ON. De remote blijft public onder v2.5. Bron 2 blijft BLOCKED op immutable opslag.

## Protocolwijziging 2026-08-28 (v2.7 live)

Protocol v2.7 legt de eigenaarsgoedgekeurde design grilling vast. First-wave officiële bestanden zijn HTML-pagina en PDF; kennisplatform `story.html`-boomplayers vallen buiten de first wave; de officiële file is de kennisplatform-freeze, geen levend Word-document. Ingest is bestandsupload of URL; een URL MUST onmiddellijk naar exacte bytes worden gesnapshot. Console-bronboom: één trunk-richtlijn plus gehashte branch-documenten; researcher-UX, geen verpleegkundig beslissingsboom. Nieuwe richtlijnversie = nieuw snapshot, object-diff; oude release blijft live tot cutover-publish. API: alleen object-level retrieve; ongepubliceerde branch-objecten abstainen ook als de trunk gepubliceerd is; `supported` draagt V- en VN-labels; DS genereert geen proza; abstain is een gesloten zinnencatalogus in de console; geen LLM in het MVP. Tenant = wie de API MAG aanroepen; alle gepubliceerde V- en VN-objecten serveren; geen ziekenhuisprotocollen, adoptielijsten of patiëntgegevens opslaan. Distributie: asset is live curatie; standaardproduct is live retrieve-and-abstain-abonnement; training alleen als tweede licentie mét live publicatiestatuscheck; eerste betalende abonnee is een Nederlands EPD/ECD; ziekenhuis-/uni-LLM-bots MAGEN op dezelfde wijze abonneren; DS bouwt die bots niet. Analytics later: welke objecten zijn gevraagd; care-impact-onderzoek hoort niet bij DS; geen federated learning. Alle v2.6-consoleregels blijven van kracht. Console blijft goedgekeurd-niet-gebouwd. G1 blijft ON. G0 Azure DEV blijft BLOCKED. Geen Vercel, Neon of LLM-vendor.

## Protocolwijziging 2026-08-28 (v2.8 live)

Protocol v2.8 legt de eigenaarsgoedgekeurde evaluatie van de grilling vast. Primaire DS-gebruikers zijn richtlijnonderzoekers (console) en B2B-abonnees (EPD, instelling, hun bot); verpleegkundigen zijn geen primaire gebruikers; ontwerp de console niet voor verpleegkundigen. Bronhiërarchie heeft twee assen: klasse/gewicht op ieder object (`richtlijn` > `handreiking` > `artikel` > `transcript`/`podcast`; zwaarder MAG niet door lichter worden gevuld; een podcast MUST NOT een richtlijn in de API vervangen, ook niet in dezelfde familie; lagere klasse MAG `supported` alleen mét klaslabel en MUST NOT een gat van een ontbrekende hogere klasse vullen als die hogere klasse in het gepubliceerde corpus bestaat) en familie/topic (bijvoorbeeld continentie) als haak, geen nieuw bestand. MVP: ingest-onderzoeker zet de familie; een branch morgen toevoegen tekent de boom niet opnieuw; verplaatsen tussen families vereist geen klinische herreview (curatoract); klasse promoveren (transcript naar richtlijn) vereist wel review; als later blijkt dat familiemoves betekenis veranderen is dat een nieuwe protocolwijziging, geen stille extra review. Console-boom = familie × klasse; ieder bestand houdt zijn eigen hash; API blijft object-level: alleen wat gevraagd is; ongepubliceerde branches abstainen. RAG op kennisplatform-HTML is niet het product; DS is de eigen live gecureerde schakel, geen scrape; train-ready gestructureerde objecten plus live publicatiestatuscheck blijven het B2B-aanbod. Bouwvolgorde: geen mockup; niet wachten op Azure of een af DS; volgende implementatie is een echte console-MVP (ingest HTML/PDF, family-tree, reviewers selecteren, review return-loop) op de bestaande kernel; Continentie bron 2 komt VIA die console binnen, niet via een parallel engineer-only pad als onderzoekerservaring; Product API bestaat al, niet eerst herbouwen; Azure DEV blijft BLOCKED onder G0; analytics later; publish-kamer klein nadat de review-loop werkt. Dit slaat duurzame immutable opslag niet over: lokale store is de console-stand-in tot G0 Azure DEV; publicatie blijft BLOCKED zonder immutable locator (G2). Console blijft goedgekeurd-niet-gebouwd in deze protocol-PR. G1 blijft ON. Remote blijft public onder v2.5. Geen Vercel, Neon of LLM-vendor. Benoemde GD-03-reviewers blijven onbezet. C5 spanning C3, eigenaarsgoedgekeurd, GD-03 ESTABLISHED, retrospectieve C5 blijft verschuldigd. Er worden geen namen verzonnen.

## Protocolwijziging 2026-08-28 (v2.9 live)

Protocol v2.9 legt de eigenaarsgoedgekeurde evaluatie vast nadat de projecteigenaar op 2026-08-28 als onderzoeker door de console-MVP klikte. De kernel (ingest, familie, review, publish, promote) is logisch juist; de UI is nog een technisch datamodel-dump (gestapelde HTML-formulieren, te veel tekst, via-negativa-copy, begraven acties, kernelwoorden op het scherm). De console MUST een taakgericht onderzoekersoppervlak zijn, geen dump van het kernel-datamodel; de primaire actie per kamer MUST visueel overduidelijk zijn; visuele hiërarchie MUST secties en volgende stappen onderscheiden; gestapelde ongelabelde HTML-formulieren van gelijk gewicht MUST NOT de console-UX zijn. Copy MUST zeggen wat de onderzoeker hier kan doen, wat daarna gebeurt en wat van hen wordt verwacht; via-negativa mag niet de primaire on-screen copy zijn (protocolgrenzen blijven wet off-screen en MAGEN één keer in een korte help). UI-vocabulaire MUST onderzoekerstaal zijn: document, titel, versie, familie, klasse, status, inleveren, review, publiceren; envelope is geen UI-term (alleen conversatiemetafoor); een onderzoeker MUST NOT een snapshot-id typen of kiezen; verplaatsen en promoveren MUST op een zichtbaar document (titel + versie + familie) werken, niet op een leeg snapshotveld, en MUST eruitzien als echte klikbare acties. Login MUST gebruikersnaam én wachtwoord vragen; geen gedeelde login; geen open registratie; wachtwoordveld MUST `type=password` zijn. Brand: console MUST de V&VN digitale stylesheet gebruiken (Rood `#E23100`, Paars `#5D3297`, Zwart `#000000`, Wit `#FFFFFF`; rood en zwart logo/accent, MUST NOT als grote achtergrond; secundair Zalm/Water/Gras/Bamboe, verzadigd voor koppen op wit, één familie per view, MUST NOT mengen; HK Grotesk body, Raleway Bold voor kopteksten, primaire knoppen en statements; gelicentieerd pad of fail-closed systeemfallback; geen fonts piraten; geen Canva-PDF in git; v&vn-beeldmerk als merk, geen behang). Nog steeds MUST NOT een verpleegkundige zorgapp, chatbot, publieke website of EPD-UI; chat is geen kamer; publicatie blijft fail-closed zonder G2. Deze delta implementeert de nieuwe UI niet. Volgende implementatie: console-UX-rewrite op de bestaande kernel (geen Azure, geen Vercel/Neon, geen mockup). G2-locator blijft de publicatieblocker. G1 blijft ON. Remote blijft public onder v2.5. Geen Vercel, Neon of LLM-vendor. Benoemde GD-03-reviewers blijven onbezet. C3, eigenaarsgoedgekeurd, GD-03 ESTABLISHED, retrospectieve C3 blijft verschuldigd. Er worden geen namen verzonnen.

## Protocolwijziging 2026-08-28 (v2.10 live)

Protocol v2.10 legt de eigenaarsgoedgekeurde evaluatie vast nadat de projecteigenaar op 2026-08-28 de v2.9-onderzoekersconsole gebruikte. De kernel blijft familie × klasse; familie blijft een haak, geen nieuw bestand. De consolekamer «Familieboom» MUST **Documentenhierarchie** heten (gewoner Nederlands; UI-vocabulaire only). Onderzoekers MUST NOT om snapshot-id's worden gevraagd. Iedere topnav-heading MUST een zichtbare wachttaak-badge (een telling, bijvoorbeeld «1») tonen als die kamer werk heeft voor de huidige gebruiker; de badge MUST afwezig of zero-hidden zijn als er niets wacht; counts MUST echt kernelwerk zijn, geen decoratie. Review = objecten/documenten in `needs_review` toegewezen aan deze reviewer (of wachtend op benoemde reviewers inclusief deze gebruiker). Publish = vastgelegde documenten die deze publisher mag overwegen; de badge MUST NOT impliceren dat publicatie G2 passeerde. Ingest = concepten/terugzendingen die op deze onderzoeker wachten als zo'n wachtrij bestaat, anders 0. Documentenhierarchie MAG documenten badgen die op familie-/klasse-actie van deze curator wachten. Accounts is identiteitsbeheer, geen chat en geen vijfde klinische kamer in plaats van ingest/review/publish. Een toegestane actor (publisher-rol; interne identity-admin voor het MVP) MUST een gebruiker kunnen aanmaken (gebruikersnaam, weergavenaam, wachtwoord), rollen toewijzen en roltoewijzing wijzigen. De rollenset blijft GESLOTEN: researcher, reviewer, publisher; operators MUST NOT nieuwe roltypes verzinnen; «een rol aanmaken» betekent één van die drie toewijzen, geen vrije RBAC-editor. Geen open registratie. Geen gedeelde login. Login blijft gebruikersnaam én wachtwoord. AI, Grok Bot, Metis, Implementation engineer en Auditor MUST NOT als vereiste reviewers aanmaakbaar zijn. Uploader MUST NOT de enige vereiste reviewer zijn. Klasse promoveren MUST een nieuwe review vereisen (al v2.8). Familiemove MUST NOT klinische herreview vereisen. Eerste bootstrap via bestaande CLI `console-account` blijft geldig. Deze delta implementeert de nieuwe UI niet. Het console-vervolg op de v2.9-UX — hernoemen, badges, Accounts-kamer — blijft verplicht; het is niet de eerstvolgende implementatie na v2.12. PR #25 (v2.9-UX-rewrite) is gemerged; die rewrite is nu in code. G1 blijft ON. Remote blijft public onder v2.5. Geen Vercel, Neon of LLM-vendor. Benoemde GD-03-reviewers blijven onbezet. C5 spanning C3, eigenaarsgoedgekeurd, GD-03 ESTABLISHED, retrospectieve C5 blijft verschuldigd. Er worden geen namen verzonnen.

## Protocolwijziging 2026-08-28 (v2.11 live component)

Protocol v2.11 legt de eigenaarsgoedgekeurde lock vast na source-locator-sparring op 2026-08-28. HTML wordt niet geheel verboden (kennisplatform-freeze is vaak HTML; Continentie first envelope zou stilvallen). Officiële first-wave HTML MUST een geüploade freeze-file zijn (exacte bytes). Live URL-HTML MUST bij ingest worden geweigerd: de kennisplatformpagina is een app-shell; line locators zouden aan de verkeerde bytes binden. PDF-upload blijft in. URL-ingest van een PDF MAG blijven (bytes zijn de PDF). URL-ingest van HTML MUST NOT. File-upload HTML/PDF en onmiddellijke byte-freeze van een geüpload bestand blijven verplicht. Dit is een begrensde supersessie van Protocol v2.7 uitsluitend voor de zin dat ingest MUST een URL accepteren voor officiële bestanden zonder HTML van PDF te onderscheiden. Kennisobjecten MUST genoeg broncontext dragen om terug te keren naar de exacte plaats in dat gehashte origineel. Schema heeft al `provenance.source_fragments` (page, bbox, source_locator). PDF-extract gebruikt `page_bbox`. HTML-extract gebruikt `web_line_range` tegen die freeze-bytes (stabiel alleen als de freeze nooit wordt gereserialiseerd). De Product API MUST NOT `supported` teruggeven als de source locator van het object ontbreekt of leeg is. Fail-closed. Abstain in plaats daarvan (cataloguszin, geen LLM). HTML line-range locators MAGEN voor first wave op geüploade freeze-bytes. Zij zijn geen excuus om een live URL te scrapen. Capture blijft geen publicatie. G2-locator blijft verplicht om te publiceren. Geen Azure/Vercel/Neon in deze delta. Deze delta implementeert die ingest-weigering en API-fail-closed niet. v2.11-kernelwerk blijft verplichte wet (live URL-HTML weigeren; fail-closed `supported` zonder locator) en is geen supersessie van v2.12 §10. PROTOCOL → tests → code later. G1 blijft ON. Remote blijft public onder v2.5. Geen Vercel, Neon of LLM-vendor. Benoemde GD-03-reviewers blijven onbezet. C3, eigenaarsgoedgekeurd, GD-03 ESTABLISHED, retrospectieve C3 blijft verschuldigd. Er worden geen namen verzonnen.

## Protocolwijziging 2026-08-28 (v2.12 live)

Protocol v2.12 legt de eigenaarsgoedgekeurde lock vast: herontwerp de vier lagen niet. Het probleem is semantische classificatie en de harde binding van review, publicatie en serving. Extractie MUST alleen structuur en provenance bepalen, niet de betekenis van een passage. Een heading MAG `object_type` heading worden. Al het overige MUST defaulten naar unclassified. De machine MAG een type voorstellen. Een menselijke reviewer MUST het definitieve object_type bevestigen vóór publicatie. Een onbevestigd voorstel MUST NOT als gepubliceerd type worden behandeld. De gesloten object-typeset is heading, definition, explanation, condition, exception, recommendation; Operators MUST NOT types verzinnen in het MVP; unclassified is de default, geen zesde advies-type. Answerability MUST vraagtype × objecttype joinen (niet alleen recommendations); supported wanneer het beschikbare, gereviewde objecttype bij de geclaimde vraag past. Alleen recommendation MAG als handelingsadvies terugkomen. Definition MAG een definitievraag beantwoorden; explanation MAG uitleg ondersteunen; condition en exception MAGEN advies begrenzen. Die andere typen MUST NOT advies-gewicht krijgen (geen advies-gewicht). Een podcast/artikel kan nog steeds geen ontbrekende richtlijn vullen (v2.8-klasse-as ongewijzigd). Cutover/publish MUST NOT envelope `review_passes` alleen vertrouwen. Minimumbinding van gepubliceerde inhoud: `object_id` + `object_version` + `canonical_object_hash` + `confirmed_object_type` + reviewer + decision; niet een envelope-vinkje. Onafhankelijkheidsregel ongewijzigd (uploader MUST NOT de enige vereiste reviewer zijn). High-risk blijft het verplichte reviewspoor nodig. Toekomstige `publish()` (nog G2-blocked) MUST dit tupel checken. De Product API blijft een afgeleide read-only laag. Serving MUST een gevalideerde gepubliceerde projectie gebruiken. Publish, withdraw en supersede MUST die projectie atomair vervangen. De API MUST alleen de huidige gepubliceerde projectie lezen. MUST NOT live governance per query reconstrueren. Een verouderde projectie na withdraw is een protocolfout. Capture is geen publicatie. G2 blokkeert echte publish nog; deze delta specificeert hoe serving MUST werken zodra er een publish-pad is, en MUST gelden voor iedere fixture/reële projectie die al in de API zit. Protocol v2.11.0 is nu een live component van deze gecombineerde baseline (geüploade freeze-HTML, weigering live URL-HTML, locators, fail-closed `supported`); v2.12 MUST NOT v2.11 als substituut voor deze semantische delta behandelen. Deze v2.11-PR herschrijft de checksumgebonden v2.12-wet niet. Azure/G2 blijven buiten deze delta: geen locator-implementatie, geen Blob, geen G2-PASS. Deze delta implementeert extract/API/console niet. Deze delta implementeert de nieuwe UI niet. Volgende implementatie (niet deze PR): Implementation engineer op de bestaande kernel — objecttaxonomie default unclassified + voorstel/bevestiging; answerability × type; review gebonden aan exacte objectversie + hash; atomaire gepubliceerde projectie en correcte withdraw/supersede; DAARNA G2/Azure. PROTOCOL → tests → code later. Geen Azure starten in deze wijziging. Het v2.10-console-vervolg blijft verplicht en is niet de eerstvolgende implementatie. v2.11-kernelwerk blijft verplichte wet en is geen supersessie van v2.12 §10. G1 blijft ON. Remote blijft public onder v2.5. Geen Vercel, Neon of LLM-vendor. Benoemde GD-03-reviewers blijven onbezet. C3 spanning C5, eigenaarsgoedgekeurd, GD-03 ESTABLISHED, retrospectieve C3 blijft verschuldigd. Er worden geen namen verzonnen.

## Protocolwijziging 2026-08-28 (v2.13 live)

Protocol v2.13 legt de eigenaarsgoedgekeurde lock vast na audit tegen live baseline v2.12.0 + v2.11.0 op `main`. Herontwerp de vier lagen niet. Eén kennisobject MUST één bevestigbare betekeniseenheid zijn; context MUST in gereviewde relaties leven, niet in een blob; de canonieke store MUST de enige bron van waarheid zijn; retrieval-index, embeddings en projecties MUST afgeleid en wegwerpbaar zijn, nooit omgekeerd. Extractie MUST op betekenisgrenzen splitsen, niet op tokenbudget; tokenbudget-chunking (300–700 / 1000 in `docs/extraction_rules_v0.1.md`) MUST NOT objectidentiteit definiëren; fusion van condition in recommendation is het verboden defaultpatroon (MUST NOT splitsen alleen wanneer splitsen één grammaticale claim zou breken). Machineclassificatie is een voorstel, nooit waarheid; unclassified blijft default tot menselijke bevestiging op die exacte objectversie; onbevestigde voorstellen MUST NOT gepubliceerd type zijn en MUST NOT `supported` zijn. De v2.12-gesloten serving-typeset blijft ongewijzigd: heading, definition, explanation, condition, exception, recommendation; unclassified is de default, geen zesde advies-type; Operators MUST NOT types verzinnen. Historische types (decision, action, score_rule, table, background, patient_information) MUST NOT MVP-servingtypes zijn. `docs/extraction_rules_v0.1.md` wijkt voor serving-taxonomie, objectgrootte/chunking en fusion. Per-type classificatieregels bepalen betekenis, in, uit, en wat ALTIJD menselijk oordeel vereist. Vanaf ieder kennisobject MUST de reviewer de exacte bronpassage kunnen openen; typebevestiging zonder die flow is niet aanvaardbaar; locators blijven Protocol v2.11; geen nieuw locatorschema. Gesloten relatieset: `applies_if`, `except_if`, `defines`, `explains`, `supported_by`, `supersedes`, `parent`/`child`; Operators MUST NOT relatietypes verzinnen; schema-v1.2-namen zijn niet de serving-wet; onbevestigde relaties MUST NOT binden. Relaties MUST op de exacte objectversie worden bevestigd met het v2.12-tupel; een gewijzigde bevestigde relatieset MUST eerdere publish-autorisatie ongeldig maken. Een gepubliceerde recommendation MUST samen met gepubliceerde `applies_if`/`except_if`-targets worden geserveerd wanneer die in de gepubliceerde projectie bestaan; objecten fuseren om context te bewaren is een protocolfout. Answerability blijft vraagtype × objecttype; relaties begrenzen advies en MUST NOT advies-gewicht geven. `exception` is ALTIJD high-risk (four-eyes). Four-eyes (tweede benoemde reviewer op het exacte objecttupel) MUST wanneer confirmed type `exception` is, `risk_level` high is, of age_boundary/dosage/unit/score_points/score_threshold/operator/contraindication/exception/escalation_decision aanwezig is. Envelope `review_passes` MUST NOT publicatie autoriseren. AI / Grok Bot / Metis / Implementation engineer / Auditor MUST NOT als vereiste reviewers meetellen, MUST NOT goedkeuren, MUST NOT publiceren. Uploader MUST NOT de enige vereiste reviewer zijn. G2 blokkeert echte `publish()` nog. Deze delta implementeert extract, relaties, open-origineel, schema of `publish()` niet. Deze delta schrijft Protocol v2.14 niet. Waar v2.13 en v2.12 §10 botsen over welke implementatie de volgende is, geldt v2.13. Volgende implementatie (niet deze PR): Implementation engineer op de bestaande kernel — atomaire split, gesloten relaties, typebevestiging, high-risk four-eyes, open-origineel; DAARNA G2/Azure. Protocol v2.14 is niet de volgende stap. PROTOCOL → tests → code later. Metis implementeert niet. Geen Azure starten in deze wijziging. Het v2.10-console-vervolg is al in code (inclusief PR #31) en wordt niet heropend. v2.11-freeze/locator en v2.12-type/projectie blijven verplichte wet. G1 blijft ON. Remote blijft public onder v2.5. Geen Vercel, Neon of LLM-vendor. Benoemde GD-03-reviewers blijven onbezet. C3 spanning C5, eigenaarsgoedgekeurd, GD-03 ESTABLISHED, retrospectieve C3 (spanning C5 voor four-eyes) blijft verschuldigd. Er worden geen namen verzonnen. Mergecommit van PR #32 is `8cfe9dc37a8f3f0bf3548a40abff41acb4fb39ce` (2026-08-28T21:41:15Z), vastgelegd in `data/assurance/protocol_v2_13_approval.json`.

## Eerstvolgende taak

Roadmapaanvulling 2026-08-31 (docs/assurance): SA-01 tot en met SA-05 maken bron-/familie-eigenaarschap, delta-review, semantisch conflictbeheer, end-to-end integriteitsreconciliatie en een afzonderlijke trainingsdatasettrack expliciet planbaar. De bijbehorende besluiten GD-08, GD-09, GD-10, GD-11 en GD-12 staan OPEN. Zij zijn geen nieuw protocol en geen geïmplementeerd productgedrag. De bestaande volgorde blijft leidend: eerst de tweekoloms reviewkaart, daarna G2. SA-01, SA-03 en SA-04 hebben een gate vóór de toepasselijke externe pilot; SA-02 volgt op bewijs uit vijf echte richtlijnen; SA-05 blokkeert trainingsexport maar niet de retrieve-and-abstain-pilot.

1. Implementation engineer op de bestaande kernel: atomaire objecten + gesloten relaties + typebevestiging + high-risk four-eyes + open-origineel zijn in code. Console review completeness (relatiebevestiging, type-select, bronpassage-gate, serving-type/locator) is in code. Eigenaarslock 2026-08-29: volgende console-implementatie (niet deze docs-PR) is alleen de tweecoloms reviewkaart — links kennisobject (type, relatietickboxes, besluit), rechts exacte bronpassage (v2.11-locators); smalle schermen stapelen. Relaties blijven voorgestelde checkboxes + bevestigen. MUST NOT een graaf-editor, drag-and-drop of pijlen-tekenen-UI. Typebevestiging blijft geblokkeerd als de passage niet open kan. Metis implementeert niet. Protocol v2.14 is niet de volgende stap.
2. G2-store staat in Azure (`aidataservice` / `canonical-sources`) en in code. G2 blijft BLOCKED tot een SHA-256-geverifieerde bron in die container ligt en is gebonden. Capture is geen publicatie. `publish()` blijft G2-BLOCKED. Geen runtime, Postgres, Foundry, Vercel, Neon of LLM starten. G2 overslaan MUST NOT.
3. Retrospectieve onafhankelijke C5-review van PR #4, PR #5, PR #16, Protocol v2.6 / PR #18, Protocol v2.7 / PR #19, Protocol v2.8 / PR #21, de console-implementatie / PR #23, de console-UX-rewrite / PR #25 en Protocol v2.10, plus retrospectieve C3-review van Protocol v2.9, Protocol v2.11, Protocol v2.12 en Protocol v2.13 (C3 spanning C5 four-eyes), volgens de GD-03-matrix; daarna named reviewers bezetten. Er worden geen namen verzonnen.
4. Holdout B onafhankelijk vergrendelen; geen tuning daarop. GD-01 en GD-02 blijven OPEN tot die gates. False support is een Fase-3-meetlat, geen stille extra gate in v2.13.
5. Na de MVP: een nieuw plan dat private hosting of een organisatieplan herstelt.
6. Pas na de MVP-gates een U1/U2-consumentencontract en partnerpilot ontwerpen. Eerste betalende abonnee is een Nederlands EPD/ECD; DS bouwt geen EPD/ECD of bots.

## Hervattingsinstructie

Bij hervatting worden achtereenvolgens volledig gelezen:

1. `PROTOCOL.md` en de gekoppelde geldende protocolspecificaties, plus het ondergeschikte record `docs/GOVERNANCE.md`;
2. `ROADMAP.md`;
3. `HANDOFF.md`;
4. de relevante acceptatie- en invarianttests;
5. pas daarna de implementatiecode.

Na iedere materiële merge wordt deze handoff in dezelfde PR bijgewerkt met: mergecommit, afgeronde gate, resterende blokkades, afwijkingen en één eerstvolgende concrete taak.
